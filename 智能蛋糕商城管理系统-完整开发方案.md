# 智能蛋糕商城管理系统 — 完整开发方案

> 文档版本：v1.0  
> 适用架构：前后端分离（FastAPI + Vue3）  
> 目标读者：全栈开发者（适合零基础起步）

---

## 一、项目整体概述

### 1.1 项目背景与意义

**背景**  
传统蛋糕店普遍采用线下经营或简单的外卖平台入驻模式，面临以下痛点：

- 订单全靠手写或外卖平台接单，容易出错、漏单
- 客户无法直观浏览全部蛋糕款式、口味和价格
- 促销活动、新品上架通知难以及时触达客户
- 没有客户数据和销售数据分析，营销决策靠感觉
- 智能客服缺失，客户咨询需要人工逐一回复，效率低

**意义**  
开发一套自有的智能蛋糕商城管理系统，可以帮助蛋糕店：

- 实现线上展示、下单、支付、配送全链路数字化
- 通过智能客服自动解答常见问题（蛋糕尺寸、价格、配送范围等），降低人工成本
- 积累客户和订单数据，用数据驱动经营决策
- 建立品牌独立的线上商城，减少对外卖平台的依赖

### 1.2 项目目标与核心亮点

**总体目标**  
构建一套功能完整、可直接商用的 B2C 蛋糕商城系统，支持管理员后台管理全店运营，普通用户在线浏览、下单、查询订单，并内置智能客服能力。

**核心亮点**

| 亮点 | 说明 |
|------|------|
| 智能客服 | 基于 LangChain 的单 Agent 架构，可回答蛋糕规格、价格、配送等常见问题，未来可扩展为多 Agent 协同 |
| 前后端分离 | FastAPI + Vue3 架构清晰，易于扩展和维护 |
| 完整订单闭环 | 从浏览 → 加购 → 下单 → 支付 → 配送状态跟踪，全链路覆盖 |
| 数据驱动运营 | 销量统计、用户管理、订单分析，辅助商家做经营决策 |
| 零基础友好 | 框架选型成熟、文档齐全、每一步从零搭建 |

### 1.3 项目适用场景

- 中小型蛋糕烘焙店自有线上商城
- 蛋糕定制工作室的订单管理系统
- 校园、社区蛋糕配送平台
- 可作为毕业设计或个人全栈项目练手

---

## 二、系统需求分析

### 2.1 功能性需求

#### 前台用户端（普通用户角色）

| 编号 | 功能模块 | 功能说明 |
|------|----------|----------|
| U-01 | 用户注册 | 手机号/邮箱注册，密码加密存储 |
| U-02 | 用户登录 | 账号密码登录，JWT Token 鉴权 |
| U-03 | 系统公告浏览 | 首页展示轮播公告/通知 |
| U-04 | 蛋糕商品浏览 | 按分类、口味、价格区间浏览 |
| U-05 | 商品详情查看 | 查看蛋糕图片、规格、价格、描述 |
| U-06 | 收货地址管理 | 新增/编辑/删除收货地址 |
| U-07 | 下单购买 | 选择规格 → 填写地址 → 提交订单 |
| U-08 | 订单查询 | 查看个人所有订单及状态 |
| U-09 | 个人信息管理 | 修改头像、昵称、联系方式 |
| U-10 | 修改密码 | 旧密码验证后更新密码 |
| U-11 | 智能客服咨询 | 与 AI 客服对话，获取商品、订单、配送帮助 |

#### 后台管理端（管理员角色）

| 编号 | 功能模块 | 功能说明 |
|------|----------|----------|
| A-01 | 管理员登录 | 独立后台登录入口 |
| A-02 | 个人资料管理 | 修改管理员昵称、头像 |
| A-03 | 修改密码 | 修改管理员登录密码 |
| A-04 | 用户管理 | 查看/搜索/禁用用户账号 |
| A-05 | 蛋糕分类管理 | 新增/编辑/删除蛋糕分类 |
| A-06 | 蛋糕信息管理 | 新增/编辑/下架蛋糕商品 |
| A-07 | 订单管理 | 查看所有订单、修改订单状态（待付款/已付款/制作中/配送中/已完成/已取消） |
| A-08 | 收货地址管理 | 查看管理用户的收货地址 |
| A-09 | 系统公告管理 | 新增/编辑/删除首页公告 |
| A-10 | 数据统计看板 | 订单总数、销售额趋势、热销蛋糕排行 |

### 2.2 非功能性需求

| 维度 | 要求 |
|------|------|
| 性能 | 页面加载 ≤ 3s；API 响应 ≤ 500ms（正常情况下） |
| 安全 | 密码 bcrypt 加密存储；JWT Token 鉴权；SQL 注入防护（ORM 自带）；XSS 过滤 |
| 兼容性 | 支持 Chrome / Edge / Safari 最新版本；移动端响应式 |
| 可扩展性 | 接口设计遵循 RESTful 规范，便于新增功能模块 |
| 可用性 | 界面简洁友好，操作流程有明确引导和反馈提示 |
| 数据准确性 | 订单金额、库存数据必须保证事务一致性 |

### 2.3 业务流程图

#### 下单流程

```
用户浏览商品 → 选择规格/数量 → 加入购物车
    ↓
确认收货地址 → 提交订单 → 订单状态：待付款
    ↓
用户付款（模拟） → 订单状态：已付款
    ↓
管理员确认 → 开始制作 → 订单状态：制作中
    ↓
制作完成 → 配送中 → 订单状态：配送中
    ↓
用户确认收货 → 订单状态：已完成
```

#### 商品上架流程

```
管理员登录后台 → 新增蛋糕分类（如：生日蛋糕、慕斯、甜点）
    ↓
填写蛋糕信息（名称、图片、价格、规格、描述）
    ↓
保存 → 上架 → 前台可见
    ↓
（可选）下架 → 前台隐藏
```

#### 智能客服对话流程

```
用户在前台点击"智能客服"按钮
    ↓
弹出对话窗口 → 输入问题
    ↓
后端 LangChain Agent 接收消息
    ↓
Agent 调用工具：
  - 查询商品信息（分类、价格、规格）
  - 查询订单状态
  - 查询配送范围/时间
  - 常识性问题直接回答
    ↓
返回回答 → 对话窗口展示
```

---

## 三、技术栈选型

### 3.1 前端技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| Vue3 | ^3.4 | 核心前端框架 |
| Vite | ^5.x | 构建工具（开发快速、热更新） |
| Vue Router | ^4.x | 前端路由管理 |
| Pinia | ^2.x | 状态管理（替代 Vuex） |
| Element Plus | ^2.x | UI 组件库（表格、表单、弹窗、菜单等） |
| Axios | ^1.x | HTTP 请求库 |
| ECharts | ^5.x | 数据统计图表（后台看板） |
| dayjs | ^1.x | 日期格式化 |

### 3.2 后端技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| Python | 3.12 | 后端编程语言 |
| FastAPI | ^0.111 | Web 框架（异步高性能） |
| Uvicorn | ^0.30 | ASGI 服务器 |
| Tortoise-ORM | ^0.21 | 异步 ORM（对接 MySQL） |
| PyMySQL | ^1.x | MySQL 驱动 |
| bcrypt | ^4.x | 密码哈希 |
| PyJWT | ^2.x | JWT Token 生成与验证 |
| LangChain | ^0.3 | 智能客服 Agent 框架 |
| OpenAI / DashScope | SDK | LLM 调用（可选通义千问或 GPT） |
| python-dotenv | ^1.x | 环境变量管理 |
| aerich | ^0.8 | Tortoise-ORM 数据库迁移工具 |

### 3.3 数据库

| 技术 | 版本 | 说明 |
|------|------|------|
| MySQL | 8.0+ | 关系型数据库 |
| Navicat 16 | 桌面工具 | 数据库可视化管理（可选） |

### 3.4 开发工具与环境

| 工具 | 说明 |
|------|------|
| PyCharm 2024.1 | Python 后端开发（推荐 Professional） |
| VS Code / WebStorm | 前端 Vue3 开发 |
| Node.js 18+ | 前端运行环境 |
| Git + GitHub | 版本控制与代码托管 |
| Postman / Apifox | 接口测试 |

### 3.5 开发环境准备清单

```bash
# 1. 检查 Python 版本
python3 --version    # 需 ≥ 3.12

# 2. 检查 Node 版本
node --version       # 需 ≥ 18

# 3. 检查 MySQL 是否启动
mysql --version

# 4. 安装后端依赖
pip install fastapi uvicorn tortoise-orm pymysql bcrypt PyJWT python-dotenv aerich langchain openai httpx

# 5. 创建前端项目
npm create vite@latest cake-shop-frontend -- --template vue
cd cake-shop-frontend
npm install
```

---

## 四、系统总体架构设计

### 4.1 前后端分离架构

```
┌─────────────────────────────────────────────────┐
│                  前端层 (Vue3)                    │
│  ┌─────────┐ ┌─────────┐ ┌─────────────────┐   │
│  │ 用户端   │ │ 管理端   │ │ 智能客服对话窗   │    │
│  │ (前台)   │ │ (后台)   │ │ (嵌入用户端)     │   │
│  └────┬────┘ └────┬────┘ └────────┬────────┘   │
│       │           │               │            │
│  ┌────┴───────────┴───────────────┴────────┐   │
│  │         Axios HTTP 请求层                │   │
│  └────────────────┬────────────────────────┘   │
└───────────────────┼────────────────────────────┘
                    │ HTTP / JSON
                    ▼
┌─────────────────────────────────────────────────┐
│               Nginx (生产环境代理)                │
└────────────────┬────────────────────────────────┘
                 │
┌────────────────┴─────────────────────────────────┐
│             后端层 (FastAPI)                      │
│  ┌──────────────────────────────────────────┐   │
│  │         API 路由层 (Routers)              │    │
│  │  /api/user  /api/cake  /api/order ...   │   │
│  └────────────────┬─────────────────────────┘   │
│                   │                              │
│  ┌────────────────┴─────────────────────────┐   │
│  │         业务逻辑层 (Services)              │    │
│  └────────────────┬─────────────────────────┘   │
│                   │                              │
│  ┌────────────────┴─────────────────────────┐   │
│  │         数据访问层 (Tortoise-ORM)          │    │
│  └────────────────┬─────────────────────────┘   │
└───────────────────┼────────────────────────────┘
                    │
┌───────────────────┴────────────────────────────┐
│                MySQL 8                          │
│  (user / cake / category / order / cart / ...) │
└────────────────────────────────────────────────┘

┌────────────────────────────────────────────────┐
│         智能客服模块 (LangChain Agent)           │
│  ┌──────────┐  ┌──────────┐  ┌────────────┐  │
│  │ LLM 调用  │  │ 工具调用  │  │ 对话历史    │   │
│  │ (GPT/通义)│  │ (查商品/  │  │ 管理       │  │
│  │          │  │  查订单)  │  │            │  │
│  └──────────┘  └──────────┘  └────────────┘  │
└────────────────────────────────────────────────┘
```

### 4.2 系统模块划分

```
智能蛋糕商城管理系统
│
├── 前台用户端 (frontend-user)
│   ├── 首页模块（公告轮播、热门蛋糕推荐）
│   ├── 商品浏览模块（分类筛选、搜索、详情）
│   ├── 购物车模块（添加、修改数量、删除）
│   ├── 订单模块（下单、支付模拟、订单查询）
│   ├── 个人中心模块（信息管理、地址管理、密码修改）
│   └── 智能客服模块（对话窗口）
│
├── 后台管理端 (frontend-admin)
│   ├── 仪表盘模块（数据统计、图表）
│   ├── 用户管理模块（查看、搜索、禁用）
│   ├── 商品管理模块（分类管理、蛋糕管理）
│   ├── 订单管理模块（订单列表、状态变更）
│   ├── 公告管理模块（新增、编辑、删除公告）
│   └── 系统管理模块（管理员资料、密码修改）
│
├── 后端 API 服务 (backend)
│   ├── auth 模块（登录注册、JWT 鉴权）
│   ├── user 模块（用户 CRUD、地址管理）
│   ├── cake 模块（分类、商品 CRUD）
│   ├── order 模块（订单 CRUD、状态管理）
│   ├── cart 模块（购物车管理）
│   ├── announcement 模块（公告管理）
│   ├── dashboard 模块（数据统计）
│   └── agent 模块（智能客服接口）
│
└── 智能客服 (agent)
    └── LangChain Agent（工具调用 + LLM 推理）
```

### 4.3 数据流转逻辑

```
用户操作 → Vue Router 路由跳转 → 组件渲染
    ↓
用户事件（点击、提交）→ Pinia Action 触发
    ↓
Axios 发送 HTTP 请求 → FastAPI 路由接收
    ↓
Service 层执行业务逻辑 → ORM 操作数据库
    ↓
返回 JSON 响应 → 前端渲染更新
```

---

## 五、详细功能模块设计

### 5.1 前台用户端功能模块

#### 5.1.1 首页模块

- **功能**：轮播公告展示、热门蛋糕推荐、分类快捷入口
- **关键组件**：`Carousel` 轮播图、`Card` 商品卡片、`Menu` 分类导航
- **数据来源**：`/api/announcements`、`/api/cakes/hot`、`/api/categories`

#### 5.1.2 商品浏览模块

- **分类展示**：左侧分类树，右侧商品列表
- **筛选功能**：按分类、价格区间、口味筛选
- **搜索功能**：关键词模糊搜索商品名称和描述
- **商品详情**：大图展示、规格选择（尺寸/口味/数量）、价格展示、加入购物车按钮

#### 5.1.3 购物车模块

- 展示已添加商品列表（缩略图、名称、规格、单价、数量、小计）
- 修改数量、删除商品
- 显示购物车总价
- 点击"去结算"进入下单页

#### 5.1.4 订单模块

- **下单页**：确认商品信息、选择收货地址、备注留言、提交订单
- **支付模拟**：点击"立即支付"模拟支付成功，订单状态更新
- **订单列表**：按状态分页显示（全部/待付款/已付款/制作中/配送中/已完成/已取消）
- **订单详情**：显示订单号、商品明细、总价、状态、物流信息

#### 5.1.5 个人中心模块

- 个人信息展示和编辑（头像、昵称、手机号、邮箱）
- 收货地址管理（CRUD，默认地址设置）
- 修改密码表单

#### 5.1.6 智能客服模块

- 右下角悬浮气泡按钮，点击展开聊天窗口
- 输入框发送问题，流式展示回复
- 常见问题快捷按钮（如"蛋糕尺寸有哪些？""今天能配送吗？"）
- 对话历史展示

### 5.2 后台管理端功能模块

#### 5.2.1 仪表盘

- **数据卡片**：总用户数、总商品数、今日订单数、本月销售额
- **趋势图**（ECharts）：近 7 天/30 天订单量趋势
- **排行榜**：销量 Top 10 蛋糕

#### 5.2.2 用户管理

- 表格展示所有用户（ID、昵称、手机号、注册时间、状态）
- 按昵称/手机号搜索
- 禁用/启用用户账号

#### 5.2.3 蛋糕分类管理

- 分类列表（ID、名称、排序号、操作）
- 新增/编辑分类（名称、排序号、说明）
- 删除分类（有关联商品时提示不可删除）

#### 5.2.4 蛋糕信息管理

- 蛋糕列表（名称、分类、价格、上架状态、操作）
- 新增蛋糕（名称、分类、规格[尺寸/口味]、价格、描述、多张图片上传、上架状态）
- 编辑蛋糕、批量上架/下架
- 支持商品图片上传和预览

#### 5.2.5 订单管理

- 所有订单列表（订单号、用户、总价、状态、下单时间、操作）
- 按订单号搜索、按状态筛选
- 修改订单状态（待付款→已付款、已付款→制作中、制作中→配送中、配送中→已完成、→已取消）
- 查看订单详情

#### 5.2.6 公告管理

- 公告列表（标题、发布时间、状态）
- 新增/编辑公告（标题、内容、是否置顶、是否发布）
- 删除公告

#### 5.2.7 管理员设置

- 修改个人资料（昵称、头像）
- 修改密码

### 5.3 智能客服功能详细设计（核心重点）

**这是本系统的核心特色功能，必须落地可行，绝不做空洞的理论。**

#### 5.3.1 基础架构

```
用户输入 → LangChain Agent (单 Agent)
              ↓
         Agent 决策（ReAct 模式）
              ↓
    ┌─────────┼─────────┐
    ▼         ▼         ▼
 查询商品  查询订单  常识回答
  (工具)   (工具)   (LLM 直接)
    │         │         │
    └─────────┼─────────┘
              ▼
         最终回复
         → 用户
```

#### 5.3.2 技术选型方案

| 组件 | 选择 | 原因 |
|------|------|------|
| Agent 框架 | LangChain 的 `create_react_agent` | 成熟稳定，入门门槛低 |
| LLM | 初期选 DashScope（通义千问）API，成本低、国内访问快 | 也可替换为 OpenAI GPT-4o-mini（更便宜） |
| 工具定义 | @tool 装饰器定义 Python 函数 | 直接调用后端 Service 层，无需额外 HTTP 调用 |
| 对话历史 | LangChain 的 `ChatMessageHistory`，存储在 Redis 或内存 | 后续可存入 MySQL |

#### 5.3.3 工具设计

| 工具名称 | 功能 | 调用示例 |
|----------|------|----------|
| `search_cakes` | 按关键词搜索蛋糕（名称、描述、分类） | "有什么水果蛋糕？" |
| `get_cake_detail` | 查询某个蛋糕的详细规格和价格 | "8寸草莓蛋糕多少钱？" |
| `get_cake_categories` | 查询所有蛋糕分类 | "你们有哪些类型的蛋糕？" |
| `get_order_status` | 查询用户某个订单的状态 | "我的订单到哪里了？" |
| `get_delivery_info` | 查询配送范围和时间 | "多久能送到？" |
| `recommend_cakes` | 根据场景推荐蛋糕 | "朋友生日送什么蛋糕好？" |

#### 5.3.4 对话流程示例

```
用户：今天能配送吗？
  ↓ Agent 调用 get_delivery_info 工具
  ↓ 返回：市区 2 小时达，郊区 3-4 小时，营业时间 9:00-21:00
  ↓ 回复：我们市区范围内下单后 2 小时左右送达，郊区 3-4 小时。
      现在下单的话今天可以送到哦，营业到晚上 9 点！

用户：有没有草莓味的蛋糕？
  ↓ Agent 调用 search_cakes("草莓")
  ↓ 返回：草莓奶油蛋糕 ¥168、草莓慕斯 ¥128、草莓千层 ¥98
  ↓ 回复：有的！我们有以下草莓味蛋糕：
      1. 草莓奶油蛋糕 ¥168
      2. 草莓慕斯 ¥128
      3. 草莓千层 ¥98
      您想了解哪一款的详细信息呢？
```

#### 5.3.5 分阶段实施

**第一阶段（MVP）**：单 Agent，2-3 个工具（search_cakes、get_cake_detail、get_delivery_info），对话不持久化。

**第二阶段**：增加 get_order_status 工具（需用户提供订单号或绑定用户身份），增加对话历史存储。

**第三阶段（扩展）**：多 Agent 协同——一个 Agent 负责商品咨询、一个负责订单售后、一个负责促销推荐，由 Supervisor Agent 分配任务。

---

## 六、数据库详细设计

### 6.1 核心数据表清单

| 表名 | 说明 | 备注 |
|------|------|------|
| `admin` | 管理员表 | 后台登录用 |
| `user` | 用户表 | 前台用户 |
| `category` | 蛋糕分类表 | 如：生日蛋糕、慕斯、甜点 |
| `cake` | 蛋糕商品表 | 核心商品信息 |
| `cake_spec` | 蛋糕规格表 | 尺寸/口味/价格变体 |
| `cart` | 购物车表 | 用户加购记录 |
| `order` | 订单表 | 核心订单信息 |
| `order_item` | 订单明细表 | 订单中的每个商品 |
| `address` | 收货地址表 | 用户收货地址 |
| `announcement` | 系统公告表 | 首页轮播内容 |
| `order_log` | 订单日志表 | 状态变更记录 |

### 6.2 各表详细设计

#### 6.2.1 admin（管理员表）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | INT | PK, AUTO_INCREMENT | 主键 |
| username | VARCHAR(50) | UNIQUE, NOT NULL | 管理员账号 |
| password | VARCHAR(255) | NOT NULL | 密码（bcrypt 哈希） |
| nickname | VARCHAR(50) | | 昵称 |
| avatar | VARCHAR(255) | | 头像 URL |
| created_at | DATETIME | NOT NULL | 创建时间 |
| updated_at | DATETIME | NOT NULL | 更新时间 |

#### 6.2.2 user（用户表）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | INT | PK, AUTO_INCREMENT | 主键 |
| username | VARCHAR(50) | UNIQUE, NOT NULL | 用户名 |
| password | VARCHAR(255) | NOT NULL | 密码（bcrypt 哈希） |
| nickname | VARCHAR(50) | | 昵称（默认"用户XXXX"） |
| phone | VARCHAR(20) | UNIQUE | 手机号 |
| email | VARCHAR(100) | | 邮箱 |
| avatar | VARCHAR(255) | | 头像 URL |
| status | TINYINT | DEFAULT 1 | 状态：1-正常 0-禁用 |
| created_at | DATETIME | NOT NULL | 注册时间 |
| updated_at | DATETIME | NOT NULL | 更新时间 |

#### 6.2.3 category（蛋糕分类表）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | INT | PK, AUTO_INCREMENT | 主键 |
| name | VARCHAR(50) | NOT NULL | 分类名称 |
| sort_order | INT | DEFAULT 0 | 排序号（小→大） |
| description | VARCHAR(255) | | 分类说明 |
| created_at | DATETIME | NOT NULL | 创建时间 |

#### 6.2.4 cake（蛋糕商品表）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | INT | PK, AUTO_INCREMENT | 主键 |
| category_id | INT | FK → category.id | 所属分类 |
| name | VARCHAR(100) | NOT NULL | 蛋糕名称 |
| description | TEXT | | 描述 |
| cover_image | VARCHAR(255) | | 封面图 URL |
| images | JSON | | 多张图片 URL 数组 |
| base_price | DECIMAL(10,2) | NOT NULL | 基础价格 |
| is_on_sale | TINYINT | DEFAULT 1 | 上架状态 |
| sales_volume | INT | DEFAULT 0 | 累计销量 |
| created_at | DATETIME | NOT NULL | 创建时间 |
| updated_at | DATETIME | NOT NULL | 更新时间 |

#### 6.2.5 cake_spec（蛋糕规格表）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | INT | PK, AUTO_INCREMENT | 主键 |
| cake_id | INT | FK → cake.id | 所属蛋糕 |
| spec_name | VARCHAR(50) | NOT NULL | 规格名称（如：6寸/8寸/10寸） |
| price | DECIMAL(10,2) | NOT NULL | 该规格价格 |
| stock | INT | DEFAULT 0 | 库存 |
| created_at | DATETIME | NOT NULL | 创建时间 |

#### 6.2.6 cart（购物车表）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | INT | PK, AUTO_INCREMENT | 主键 |
| user_id | INT | FK → user.id | 用户 ID |
| cake_id | INT | FK → cake.id | 蛋糕 ID |
| spec_id | INT | FK → cake_spec.id | 规格 ID |
| quantity | INT | DEFAULT 1 | 数量 |
| created_at | DATETIME | NOT NULL | 添加时间 |

#### 6.2.7 order（订单表）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | INT | PK, AUTO_INCREMENT | 主键 |
| order_no | VARCHAR(50) | UNIQUE, NOT NULL | 订单号（规则：yyyyMMdd+8位随机数） |
| user_id | INT | FK → user.id | 用户 ID |
| address_id | INT | FK → address.id | 收货地址 ID |
| total_amount | DECIMAL(10,2) | NOT NULL | 订单总价 |
| status | VARCHAR(20) | NOT NULL, DEFAULT 'pending' | 状态：pending/paid/making/delivering/completed/cancelled |
| remark | VARCHAR(500) | | 买家留言 |
| paid_at | DATETIME | | 付款时间 |
| delivered_at | DATETIME | | 配送时间 |
| completed_at | DATETIME | | 完成时间 |
| created_at | DATETIME | NOT NULL | 下单时间 |
| updated_at | DATETIME | NOT NULL | 更新时间 |

#### 6.2.8 order_item（订单明细表）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | INT | PK, AUTO_INCREMENT | 主键 |
| order_id | INT | FK → order.id | 所属订单 |
| cake_id | INT | FK → cake.id | 蛋糕 ID |
| cake_name | VARCHAR(100) | NOT NULL | 快照：蛋糕名称 |
| spec_name | VARCHAR(50) | NOT NULL | 快照：规格 |
| price | DECIMAL(10,2) | NOT NULL | 快照：单价 |
| quantity | INT | NOT NULL | 数量 |
| subtotal | DECIMAL(10,2) | NOT NULL | 小计价格 |

#### 6.2.9 address（收货地址表）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | INT | PK, AUTO_INCREMENT | 主键 |
| user_id | INT | FK → user.id | 用户 ID |
| name | VARCHAR(50) | NOT NULL | 收货人姓名 |
| phone | VARCHAR(20) | NOT NULL | 联系电话 |
| province | VARCHAR(50) | NOT NULL | 省 |
| city | VARCHAR(50) | NOT NULL | 市 |
| district | VARCHAR(50) | NOT NULL | 区 |
| detail | VARCHAR(255) | NOT NULL | 详细地址 |
| is_default | TINYINT | DEFAULT 0 | 是否默认地址 |
| created_at | DATETIME | NOT NULL | 创建时间 |

#### 6.2.10 announcement（系统公告表）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | INT | PK, AUTO_INCREMENT | 主键 |
| title | VARCHAR(100) | NOT NULL | 公告标题 |
| content | TEXT | NOT NULL | 公告内容 |
| cover_image | VARCHAR(255) | | 轮播图 URL |
| is_published | TINYINT | DEFAULT 1 | 是否发布 |
| is_top | TINYINT | DEFAULT 0 | 是否置顶 |
| created_at | DATETIME | NOT NULL | 创建时间 |
| updated_at | DATETIME | NOT NULL | 更新时间 |

#### 6.2.11 order_log（订单日志表）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | INT | PK, AUTO_INCREMENT | 主键 |
| order_id | INT | FK → order.id | 订单 ID |
| from_status | VARCHAR(20) | | 变更前状态 |
| to_status | VARCHAR(20) | NOT NULL | 变更后状态 |
| operator | VARCHAR(50) | | 操作人 |
| remark | VARCHAR(255) | | 备注 |
| created_at | DATETIME | NOT NULL | 创建时间 |

### 6.3 数据库约束与优化方案

| 项目 | 方案 |
|------|------|
| 索引 | `order.order_no` 唯一索引；`order.user_id` 普通索引；`cake.category_id` 普通索引；`cart.user_id` 普通索引 |
| 外键约束 | 建议应用层维护一致性（Tortoise-ORM 通过关联字段处理），MySQL 层面可加外键但不强制 |
| 金额字段 | 统一使用 `DECIMAL(10,2)`，避免浮点数精度问题 |
| 图片存储 | 本地存储路径或对象存储（OSS），数据库存 URL 路径 |
| 软删除 | 暂不实现软删除，重要数据（订单）不可物理删除 |

---

## 七、后端 API 接口设计

### 7.1 统一返回格式

所有 API 响应统一格式：

```json
{
  "code": 200,
  "message": "success",
  "data": { ... }
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| code | int | 状态码：200 成功，400 参数错误，401 未授权，403 禁止访问，404 未找到，500 服务器错误 |
| message | string | 提示信息 |
| data | object/array | 返回数据 |

分页接口额外返回：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "items": [...],
    "total": 100,
    "page": 1,
    "page_size": 10
  }
}
```

### 7.2 所有模块接口清单

#### 7.2.1 认证模块 `/api/auth`

| 方法 | 路径 | 说明 | 参数 | 返回 |
|------|------|------|------|------|
| POST | /api/auth/register | 用户注册 | username, password, confirm_password, phone? | user info |
| POST | /api/auth/login | 用户登录 | username, password | { token, user } |
| POST | /api/auth/admin/login | 管理员登录 | username, password | { token, admin } |
| GET | /api/auth/me | 获取当前登录用户信息 | Header: Bearer Token | user info |
| PUT | /api/auth/password | 修改密码（登录态） | old_password, new_password | success |

#### 7.2.2 用户模块 `/api/users`

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | /api/users | 用户列表（分页） | admin |
| GET | /api/users/{id} | 用户详情 | admin |
| PUT | /api/users/{id}/status | 禁用/启用用户 | admin |
| PUT | /api/users/profile | 修改个人资料 | user |
| GET | /api/users/{id}/addresses | 用户地址列表 | user |
| POST | /api/users/avatar | 上传头像 | user |

#### 7.2.3 分类模块 `/api/categories`

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | /api/categories | 分类列表 | 公开 |
| GET | /api/categories/{id} | 分类详情 | 公开 |
| POST | /api/categories | 新增分类 | admin |
| PUT | /api/categories/{id} | 编辑分类 | admin |
| DELETE | /api/categories/{id} | 删除分类 | admin |

#### 7.2.4 蛋糕商品模块 `/api/cakes`

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | /api/cakes | 蛋糕列表（分页、分类筛选、搜索、价格区间） | 公开 |
| GET | /api/cakes/hot | 热门蛋糕 Top N | 公开 |
| GET | /api/cakes/{id} | 蛋糕详情（含规格列表） | 公开 |
| POST | /api/cakes | 新增蛋糕 | admin |
| PUT | /api/cakes/{id} | 编辑蛋糕 | admin |
| DELETE | /api/cakes/{id} | 删除蛋糕 | admin |
| PUT | /api/cakes/{id}/status | 上架/下架 | admin |

#### 7.2.5 购物车模块 `/api/cart`

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | /api/cart | 获取购物车列表 | user |
| POST | /api/cart | 添加到购物车 | user |
| PUT | /api/cart/{id} | 修改数量 | user |
| DELETE | /api/cart/{id} | 删除购物车项 | user |
| DELETE | /api/cart | 清空购物车 | user |

#### 7.2.6 订单模块 `/api/orders`

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | /api/orders | 当前用户订单列表（分页、状态筛选） | user |
| GET | /api/orders/admin | 所有订单列表 | admin |
| GET | /api/orders/{id} | 订单详情 | user/admin |
| POST | /api/orders | 创建订单（从购物车） | user |
| PUT | /api/orders/{id}/pay | 模拟支付 | user |
| PUT | /api/orders/{id}/cancel | 取消订单 | user |
| PUT | /api/orders/{id}/confirm | 确认收货 | user |
| PUT | /api/orders/{id}/status | 修改订单状态（管理端） | admin |

#### 7.2.7 地址模块 `/api/addresses`

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | /api/addresses | 获取当前用户地址列表 | user |
| POST | /api/addresses | 新增地址 | user |
| PUT | /api/addresses/{id} | 编辑地址 | user |
| DELETE | /api/addresses/{id} | 删除地址 | user |
| PUT | /api/addresses/{id}/default | 设为默认地址 | user |

#### 7.2.8 公告模块 `/api/announcements`

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | /api/announcements | 公告列表（公开取已发布，admin 取全部） | 公开 |
| GET | /api/announcements/{id} | 公告详情 | 公开 |
| POST | /api/announcements | 新增公告 | admin |
| PUT | /api/announcements/{id} | 编辑公告 | admin |
| DELETE | /api/announcements/{id} | 删除公告 | admin |

#### 7.2.9 仪表盘模块 `/api/dashboard`

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | /api/dashboard/stats | 数据概览（用户数/商品数/订单数/销售额） | admin |
| GET | /api/dashboard/orders/trend | 订单趋势（天/周/月） | admin |
| GET | /api/dashboard/cakes/top | 销量 Top N 蛋糕 | admin |

#### 7.2.10 智能客服模块 `/api/agent`

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| POST | /api/agent/chat | 发送消息给智能客服 | user（登录可选） |
| POST | /api/agent/chat/stream | 流式对话（SSE） | user（登录可选） |

**chat 接口参数示例：**

```json
{
  "message": "有没有水果蛋糕？",
  "session_id": "abc123",
  "user_id": 1
}
```

**返回示例：**

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "reply": "有的！我们有以下水果蛋糕：\\n1. 草莓奶油蛋糕 ¥168\\n2. 芒果慕斯 ¥128\\n3. 水果拼盘蛋糕 ¥198\\n请问您对哪款感兴趣？",
    "session_id": "abc123"
  }
}
```

---

## 八、项目开发进度计划

### 8.1 总体阶段划分

| 阶段 | 周期 | 核心产出 | 里程碑 |
|------|------|----------|--------|
| 第一阶段：基础搭建 | 第 1-3 天 | 项目初始化、数据库建表、前后端框架搭建 | 前后端能成功联调 |
| 第二阶段：核心功能 | 第 4-10 天 | 用户模块、商品模块、分类模块、购物车 | 前台可浏览和加购 |
| 第三阶段：订单系统 | 第 11-15 天 | 订单模块、地址模块、后台订单管理 | 完整下单流程跑通 |
| 第四阶段：管理后台 | 第 16-19 天 | 后台仪表盘、用户管理、公告管理 | 后台功能完整可用 |
| 第五阶段：智能客服 | 第 20-24 天 | LangChain Agent 集成、对话窗口 | 智能客服可回答问题 |
| 第六阶段：测试与部署 | 第 25-30 天 | 功能测试、修复 Bug、本地部署文档 | 系统可上线试运行 |

### 8.2 每日任务分解

#### 第一阶段（第 1-3 天）：基础搭建

**第 1 天：项目初始化与环境配置**
- 创建项目目录结构（backend / frontend-user / frontend-admin）
- 初始化后端 FastAPI 项目，配置 Uvicorn
- 配置 Tortoise-ORM 连接 MySQL
- 运行 aerich init 初始化数据库迁移
- 初始化前端 Vue3 + Vite + Element Plus 项目

**第 2 天：数据库设计与用户认证**
- 创建所有数据库 Model（admin, user, category, cake, cake_spec, cart, order, order_item, address, announcement, order_log）
- 实现注册接口（密码 bcrypt 加密）
- 实现登录接口（JWT Token 签发）
- 实现 JWT 中间件（依赖注入鉴权）

**第 3 天：前端框架与登录注册**
- 配置 Vue Router（前台路由：首页/商品/购物车/个人中心；后台路由：仪表盘/用户/商品/订单/公告）
- 配置 Pinia 状态管理（userStore、cartStore）
- 封装 Axios 请求工具（baseURL、拦截器、统一错误处理）
- 实现登录页和注册页
- 实现前后端联调验证

#### 第二阶段（第 4-10 天）：核心功能

**第 4 天：蛋糕分类管理（后端+后台前端）**
- 分类 CRUD 接口
- 后台分类管理页面（表格、新增/编辑对话框、删除确认）

**第 5-6 天：蛋糕商品管理（后端+后台前端）**
- 蛋糕 CRUD 接口（含分类关联、规格嵌套）
- 后台蛋糕管理页面（表格、表单、图片上传、上架/下架）
- 图片上传接口

**第 7-8 天：前台商品展示（前端）**
- 首页设计（轮播图、热门推荐、分类导航）
- 商品列表页（分类筛选、搜索、分页）
- 商品详情页（大图、规格选择、加入购物车）

**第 9-10 天：购物车功能（后端+前端）**
- 购物车接口（增删改查）
- 前端购物车页面（商品列表、数量修改、合计计算）
- 购物车状态管理（Pinia）

#### 第三阶段（第 11-15 天）：订单系统

**第 11 天：地址管理（后端+前端）**
- 地址接口（增删改查、设置默认）
- 前端地址管理页面

**第 12-13 天：订单接口（后端）**
- 创建订单（从购物车生成，事务保证）
- 订单列表/详情接口
- 模拟支付接口
- 订单状态变更接口
- 订单日志记录

**第 14-15 天：下单流程（前端）**
- 结算页面（确认商品/选择地址/留言/提交）
- 支付页面（模拟支付按钮）
- 订单列表页（按状态切换）
- 订单详情页

#### 第四阶段（第 16-19 天）：管理后台

**第 16 天：后台订单管理**
- 所有订单列表（按状态筛选、搜索）
- 订单详情查看
- 订单状态变更操作

**第 17 天：后台用户管理**
- 用户列表
- 搜索、禁用/启用

**第 18 天：后台仪表盘**
- 数据统计接口
- ECharts 图表展示（订单趋势、销量排行、数据卡片）

**第 19 天：公告管理与管理员设置**
- 公告管理前后端
- 管理员个人资料修改和密码修改

#### 第五阶段（第 20-24 天）：智能客服

**第 20 天：LangChain 环境搭建**
- 安装 langchain、openai/dashscope SDK
- 配置 LLM 连接（使用通义千问 API 或 GPT API）
- 编写基础 Agent（实现简单的问候、常识问答）

**第 21 天：工具开发**
- 实现 search_cakes 工具（调用蛋糕 Service）
- 实现 get_cake_detail 工具
- 实现 get_cake_categories 工具
- 实现 get_delivery_info 工具（返回预设规则）
- 实现 recommend_cakes 工具

**第 22 天：Agent 集成与优化**
- 将多个工具注册到 Agent
- 配置 System Prompt（明确角色为蛋糕店客服）
- 调试 Agent 回复质量
- 处理工具调用失败时的 Fallback

**第 23 天：前端对话窗口**
- 对话气泡 UI 组件
- 消息发送与接收
- 流式打字效果
- 常见问题快捷按钮
- 对话历史加载

**第 24 天：智能客服联调与优化**
- 前后端联调
- 边界情况处理（长文本、无结果、模糊问题）
- 对话风格调整

#### 第六阶段（第 25-30 天）：测试与部署

**第 25-26 天：功能测试**
- 逐模块测试所有接口
- 前端交互测试
- 边界场景测试（空数据、大数量、异常操作）
- Bug 修复

**第 27-28 天：压力测试与优化**
- 使用 Locust 对核心接口做简单压力测试
- SQL 查询优化（添加索引、减少 N+1 查询）
- 前端打包优化

**第 29-30 天：部署与文档**
- 编写部署文档（本地 Docker / 服务器手动部署）
- 编写使用说明
- 线上试运行

---

## 九、系统测试方案

### 9.1 功能测试

| 测试模块 | 测试项 | 预期结果 |
|----------|--------|----------|
| 注册 | 正确信息注册 | 注册成功，密码加密 |
| 注册 | 重复用户名 | 提示用户名已存在 |
| 登录 | 正确账号密码 | 返回 Token |
| 登录 | 错误密码 | 提示密码错误 |
| 商品浏览 | 按分类筛选 | 只显示该分类商品 |
| 商品浏览 | 搜索关键词 | 返回匹配结果 |
| 购物车 | 添加商品到购物车 | 购物车列表更新 |
| 购物车 | 修改数量 | 小计和总价重新计算 |
| 下单 | 有效信息下单 | 生成订单，购物车清空 |
| 下单 | 地址为空 | 提示选择地址 |
| 订单状态 | 模拟支付 | 状态从 pending → paid |
| 订单状态 | 管理员修改状态 | 状态正确变更 |
| 智能客服 | 询问蛋糕 | 返回相关蛋糕信息 |
| 智能客服 | 询问配送 | 返回配送规则 |

### 9.2 压力测试方案

- **工具**：Locust（Python）
- **测试接口**：商品列表、商品详情、登录
- **模拟用户数**：50-100 并发用户
- **关注指标**：响应时间 P95 < 1s，错误率 < 1%

### 9.3 Bug 修复流程

1. 发现 Bug → 记录到 Issues（GitHub 或本地文档）
2. 标记优先级（P0 阻塞 / P1 严重 / P2 一般 / P3 轻微）
3. 修复 → 自测 → 回归测试 → 关闭
4. P0/P1 必须在当阶段修复，P2/P3 可在下一阶段修复

---

## 十、部署与上线方案

### 10.1 本地部署

```bash
# 1. 启动 MySQL
mysql -u root -p
CREATE DATABASE cake_shop CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

# 2. 启动后端
cd backend
cp .env.example .env   # 编辑数据库连接信息
aerich migrate         # 执行数据库迁移
aerich upgrade
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000

# 3. 启动前端（用户端）
cd frontend-user
npm run dev

# 4. 启动前端（管理端）
cd frontend-admin
npm run dev
```

### 10.2 服务器部署

**方案：Nginx 反向代理 + Uvicorn + MySQL**

```nginx
# Nginx 配置示例
server {
    listen 80;
    server_name cake-shop.example.com;

    # 前端用户端
    location / {
        root /var/www/cake-shop-frontend/dist;
        try_files $uri $uri/ /index.html;
    }

    # 后端 API
    location /api/ {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # 静态文件（上传的图片等）
    location /static/ {
        alias /var/www/cake-shop-backend/static/;
    }
}
```

**后端生产启动：**

```bash
# 使用 systemd 管理
[Unit]
Description=Cake Shop Backend
After=network.target

[Service]
User=www-data
WorkingDirectory=/var/www/cake-shop-backend
ExecStart=/usr/local/bin/uvicorn app.main:app --host 127.0.0.1 --port 8000 --workers 4
Restart=always

[Install]
WantedBy=multi-user.target
```

### 10.3 部署检查清单

- [ ] MySQL 字符集设置为 utf8mb4
- [ ] 生产环境关闭 FastAPI 的 debug 模式
- [ ] JWT SECRET_KEY 更换为高强度随机字符串
- [ ] 配置 CORS 只允许前端域名
- [ ] 上传图片限制大小（建议 ≤ 5MB）
- [ ] 数据库定期备份（crontab 每日备份）

---

## 十一、项目难点与解决方案

### 难点 1：图片上传与存储

**问题**：开发环境图片存本地，生产环境需迁移到 OSS。

**方案**：
- 开发阶段：将图片上传到 `backend/static/uploads/`，配置静态文件路由
- 生产阶段：替换为阿里云 OSS / 腾讯云 COS SDK，只需修改上传 Service，接口不变

### 难点 2：订单状态一致性与并发

**问题**：多人同时下单可能导致库存超卖或状态错乱。

**方案**：
- 下单使用数据库事务（Tortoise-ORM 的 `in_transaction`）
- 订单状态变更使用乐观锁（通过 `updated_at` 字段判断）
- 库存扣减使用 `UPDATE ... WHERE stock >= quantity` 原子操作

### 难点 3：智能客服回复质量

**问题**：LLM 可能产生幻觉，回答不准确的商品信息。

**方案**：
- 所有商品数据查询通过工具函数从数据库获取，LLM 只负责理解用户意图和格式化输出
- System Prompt 强约束："你是一个蛋糕店客服助手，所有商品信息必须调用工具获取，不要编造信息"
- 如果工具查询无结果，必须如实告知用户"没有找到相关信息"

### 难点 4：零基础学习曲线

**问题**：新手面对 FastAPI + Vue3 + LangChain 可能觉得吃力。

**方案**：
- 项目目录结构保持清晰简洁，每个模块职责单一
- 提供详细的代码注释
- 使用成熟的第三方库（Element Plus 降低 UI 开发难度）
- 每一步先写后端，再写前端，前后端联调验证
- 开发环境提供 hot reload（后端 uvicorn --reload，前端 Vite 自动刷新）

### 难点 5：前后端联调跨域

**问题**：Vite 开发端口（5173）与 FastAPI 端口（8000）不同，存在跨域。

**方案**：
- 开发环境：FastAPI 添加 `CORSMiddleware`，允许 localhost:5173
- 生产环境：Nginx 同源代理，无需跨域

---

## 十二、项目总结与扩展方向

### 12.1 项目总结

本方案从零完整规划了一套智能蛋糕商城管理系统，涵盖：

- **12 张核心数据表**，覆盖用户、商品、订单、日志等全链路
- **10 个 API 模块**，80+ 个接口，全部遵循 RESTful 规范
- **前后端分离架构**，Vue3 前台 + Vue3 后台 + FastAPI 后端
- **智能客服模块**，基于 LangChain Agent 架构，从单 Agent 起步，可平滑扩展到多 Agent
- **30 天开发计划**，每天任务明确，适合新手按节奏完成

整个方案的核心关键词是 **可落地** 和 **实用**：
- 不谈空泛的理论，每项设计都对应蛋糕店真实业务场景
- 智能客服不是噱头，而是通过工具调用+LLM 的务实组合，解决常见咨询问题
- 技术选型成熟且商业友好，FastAPI 高性能、Vue3 生态活跃、MySQL 稳定可靠

### 12.2 后续扩展方向

| 方向 | 内容 | 优先级 |
|------|------|--------|
| 在线支付集成 | 接入支付宝/微信支付 H5 接口 | 高 |
| 多 Agent 智能客服 | 从单 Agent 扩展到 Supervisor + Worker 架构 | 中 |
| 短信通知 | 订单状态变更时短信通知用户 | 中 |
| 营销工具 | 优惠券系统、积分系统、拼团功能 | 中 |
| 小程序端 | 基于 uniapp 开发微信小程序版 | 中 |
| 配送管理 | 接入第三方配送平台（达达、闪送）API | 低 |
| 会员体系 | 会员等级、生日特权、积分兑换 | 低 |
| 数据分析 | 用户画像、复购率分析、RFM 模型 | 低 |

---

*— 方案结束 —*
