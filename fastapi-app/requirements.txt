# ============================================================
# FastAPI 应用 Python 依赖
# ------------------------------------------------------------
# 锁定策略：~=（兼容同 major.minor 的 patch 升级）为主，
#           ==（精确锁定）用于历史踩过坑的包。
# Python 版本要求：>=3.10（auth.py 用了 PEP 604 的 dict | None 语法）
#
# 安装：
#   python3 -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt
# ============================================================

# ---- Web 框架 ----
fastapi~=0.124.2
uvicorn~=0.38.0
starlette~=0.50.0
python-multipart~=0.0.20

# ---- ORM / MySQL ----
# tortoise-orm 0.25 系列内 model_validate / pydantic_model_creator 接口稳定
tortoise-orm~=0.25.1
aiomysql~=0.3.2

# ---- 配置 / 校验 ----
pydantic~=2.12.5
python-dotenv~=1.0.0
cryptography~=43.0.0

# ---- LLM / Embedding ----
# dashscope 1.10-1.26 之间接口有微调，1.26 验证可用，下限抬到 1.26
dashscope~=1.26.0
langchain==1.3.14
langchain-core~=1.5.3
langchain-openai==1.4.1

# ---- 向量库 / RAG ----
# chromadb 0.x → 1.x 是破坏性大版本升级（API 重构），必须收紧到 1.5.x
chromadb~=1.5.9
langchain-text-splitters~=1.1.2
PyPDF2~=3.0.1
python-docx~=1.2.0

# ---- 鉴权 ----
python-jose[cryptography]~=3.3.0
# bcrypt 5.x 改了 hashpw 返回类型与字节处理；4.x 系列验证可用
bcrypt~=4.2.0
