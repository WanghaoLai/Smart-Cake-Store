# 智能宠物商城管理系统 — 完整开发方案

> **项目名称**：智能宠物商城管理系统（PetMall）
> **开发模式**：前后端分离
> **核心框架**：FastAPI + Vue3 + MySQL8 + LangChain
> **适用规模**：商业小型项目，内容详细、落地可开发

---

## 一、项目整体概述

### 1.1 项目背景与意义

随着国内宠物经济持续升温，2024 年中国宠物市场规模已突破 3000 亿元，线上宠物用品销售渠道增长迅猛。传统宠物店铺依赖线下经营、人工记账，存在库存混乱、客户流失、售后响应慢等痛点。与此同时，大语言模型（LLM）技术逐步成熟，智能客服已在电商场景中展现出显著的降本增效能力。

本项目旨在为中小型宠物店铺打造一套**前后端分离、轻量部署、内置 AI 智能客服**的商城管理系统，帮助商家以较低成本实现线上销售、订单管理和智能售后，同时为用户提供流畅的购物体验和即时的 AI 宠物咨询服务。

### 1.2 项目目标与核心亮点

**核心目标：**
- 提供完整的宠物商品线上销售闭环（浏览 → 加购 → 下单 → 支付 → 售后）
- 后台管理端覆盖商品、订单、用户、公告等全链路运营需求
- 内置 AI 智能客服，支持宠物喂养咨询、商品推荐等场景
- 代码结构清晰，适合新人学习全栈开发全流程

**核心亮点：**

| 亮点 | 说明 |
|------|------|
| 前后端分离 | Vue3 SPA + FastAPI RESTful API，职责清晰，便于独立迭代 |
| AI 智能客服 | 基于 LangChain + 国内大模型（文心/通义），支持多轮对话与知识库检索 |
| 开箱即用 | Docker 一键部署，本地开发环境 5 分钟搭建完成 |
| 代码规范 | 统一 API 返回格式、JWT 鉴权、Tortoise-ORM 数据模型，新人友好 |

### 1.3 项目适用场景

- 个体宠物店主希望开设线上商城，预算有限，需要低成本快速上线
- 小型宠物连锁店需要统一管理多店商品与订单
- 高校全栈开发课程实训项目，涵盖前后端 + AI 三大技术方向
- 个人开发者作为作品集项目，展示完整全栈开发能力

---

## 二、系统需求解析

### 2.1 功能性需求

#### 2.1.1 前台用户端（普通用户）

| 编号 | 功能模块 | 功能描述 |
|------|----------|----------|
| U01 | 用户注册 | 支持手机号/邮箱注册，密码加密存储，注册后自动登录 |
| U02 | 用户登录 | 支持账号密码登录，JWT Token 鉴权，Token 自动刷新 |
| U03 | 首页展示 | 轮播公告、热门商品推荐、分类导航入口 |
| U04 | 商品浏览 | 按分类筛选、关键词搜索、价格排序、分页加载 |
| U05 | 商品详情 | 商品图片轮播、价格、库存、描述、用户评价展示 |
| U06 | 收货地址管理 | 新增/编辑/删除收货地址，支持设置默认地址 |
| U07 | 购物车 | 添加商品、修改数量、删除商品、全选/取消全选 |
| U08 | 下单购买 | 选择收货地址、确认订单信息、提交订单 |
| U09 | 订单管理 | 查看全部订单、按状态筛选（待付款/待发货/已完成/已取消） |
| U10 | 个人中心 | 查看/编辑个人信息，修改密码 |
| U11 | AI 客服 | 智能问答窗口，支持宠物喂养咨询、商品推荐、订单查询 |

#### 2.1.2 后台管理端（管理员）

| 编号 | 功能模块 | 功能描述 |
|------|----------|----------|
| A01 | 管理员登录 | 独立登录入口，JWT 鉴权，区分超级管理员与普通管理员 |
| A02 | 管理员信息管理 | 管理员列表、新增/编辑/删除管理员账号 |
| A03 | 用户信息管理 | 用户列表、搜索、禁用/启用用户账号 |
| A04 | 宠物分类管理 | 分类的增删改查，支持二级分类 |
| A05 | 宠物商品管理 | 商品的上架/下架、编辑信息、库存管理、图片上传 |
| A06 | 订单管理 | 订单列表、按状态筛选、发货操作、订单详情查看 |
| A07 | 收货地址管理 | 查看用户收货地址（只读，用于客服核实） |
| A08 | 系统公告管理 | 公告的发布/编辑/删除，支持富文本内容 |
| A09 | 销量统计 | 按日/周/月统计销售额、订单量，图表可视化展示 |
| A10 | 轮播管理 | 首页轮播图的上传、排序、启用/禁用 |
| A11 | 管理员个人中心 | 修改个人信息、修改密码 |

### 2.2 非功能性需求

| 维度 | 要求 |
|------|------|
| 性能 | 首页加载时间 < 2 秒，API 响应时间 < 500ms（95 分位），支持 200 并发用户 |
| 安全 | 密码使用 bcrypt 加密，JWT Token 有效期 2 小时 + 自动刷新，SQL 注入防护（ORM 层面），XSS 防护（前端转义），CORS 白名单控制 |
| 兼容性 | 前端兼容 Chrome、Safari、Firefox 最新两个版本，移动端响应式适配（Element-Plus 自带） |
| 可扩展性 | 后端采用路由分层设计，新增业务模块只需添加对应 Router + Service + Model；AI 客服模块预留多 Agent 扩展接口 |
| 可维护性 | 统一 API 返回格式，统一异常处理中间件，代码注释覆盖率 > 60% |

### 2.3 业务流程梳理

#### 2.3.1 用户下单流程

```
用户浏览商品
    ↓
选择规格 → 加入购物车
    ↓
进入购物车 → 勾选商品 → 点击结算
    ↓
选择收货地址 → 确认订单信息
    ↓
提交订单 → 系统扣减库存 → 生成订单记录（状态：待付款）
    ↓
模拟支付（本项目不做真实支付） → 订单状态变为"已付款"
    ↓
管理员后台查看 → 发货操作 → 订单状态变为"已发货"
    ↓
用户确认收货 → 订单状态变为"已完成"
```

#### 2.3.2 商品上架流程

```
管理员登录后台
    ↓
进入商品管理 → 点击"新增商品"
    ↓
填写商品信息（名称、分类、价格、库存、描述）
    ↓
上传商品图片（支持多图，第一张为主图）
    ↓
选择上架状态 → 提交保存
    ↓
商品在前台商城可见
```

#### 2.3.3 AI 客服对话流程

```
用户打开客服窗口
    ↓
输入问题（如"金毛幼犬怎么喂养？"）
    ↓
前端发送请求 → 后端 AI Service 接收
    ↓
LangChain 检索知识库（宠物喂养指南、商品信息）
    ↓
组装 Prompt（系统提示词 + 检索结果 + 用户问题）
    ↓
调用国内大模型 API（文心/通义）→ 返回回答
    ↓
前端流式渲染回答内容
```

---

## 三、技术栈选型

### 3.1 前端技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| Vue 3 | ^3.4 | 核心框架，Composition API + `<script setup>` 语法 |
| Vue Router | ^4.x | 路由管理，支持路由守卫（登录鉴权） |
| Pinia | ^2.x | 状态管理，替代 Vuex，更轻量 |
| Element Plus | ^2.7 | UI 组件库，提供表格、表单、对话框等基础组件 |
| Axios | ^1.7 | HTTP 请求库，封装拦截器处理 Token 刷新 |
| Vite | ^5.x | 构建工具，开发热更新快，生产打包体积小 |
| ECharts | ^5.5 | 图表库，用于后台销量统计可视化 |

### 3.2 后端技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| Python | 3.12 | 运行环境 |
| FastAPI | ^0.111 | Web 框架，自动生成 OpenAPI 文档，异步性能优秀 |
| Tortoise-ORM | ^0.21 | 异步 ORM，类 Django ORM 语法，与 FastAPI 配合良好 |
| Pydantic | ^2.x | 数据校验，FastAPI 内置依赖 |
| python-jose | ^3.3 | JWT Token 生成与验证 |
| passlib[bcrypt] | ^1.7 | 密码加密 |
| aiofiles | ^24.1 | 异步文件上传处理 |
| LangChain | ^0.2 | AI 应用框架，链式调用 LLM |
| LangGraph | ^0.1 | Agent 编排框架，支持有状态多轮对话 |
| Uvicorn | ^0.30 | ASGI 服务器，FastAPI 官方推荐 |

### 3.3 AI 智能客服技术方案

| 技术 | 说明 |
|------|------|
| LangChain | 核心编排框架，负责 Prompt 模板、知识库检索、Chain 组装 |
| LangGraph | Agent 状态管理，支持多轮对话上下文，后续扩展多 Agent 协作 |
| 国内大模型 API | 首选通义千问（DashScope API），备选文心一言（ERNIE Bot API） |
| 向量数据库 | ChromaDB（本地轻量），存储宠物知识库 Embedding，支持语义检索 |
| 知识库内容 | 宠物喂养指南、常见疾病护理、商品使用说明（Markdown 文件导入） |

**AI 客服模块架构（单 Agent 阶段）：**

```
用户输入
    ↓
LangChain RetrievalQA Chain
    ↓
┌───────────────────────────────┐
│ 1. 文本 Embedding（通义 Embedding API）  │
│ 2. ChromaDB 相似度检索 Top-K           │
│ 3. 组装 Prompt（系统人设 + 检索结果 + 问题）│
│ 4. 调用通义千问 Chat API               │
│ 5. 流式返回回答                        │
└───────────────────────────────┘
    ↓
前端流式渲染
```

### 3.4 数据库

| 技术 | 版本 | 说明 |
|------|------|------|
| MySQL | 8.0 | 主数据库，InnoDB 引擎，UTF-8mb4 字符集 |
| Redis | 7.x（可选） | 缓存热门商品、Session、验证码，初期可不引入，后期按需扩展 |

### 3.5 开发工具与环境

| 工具 | 用途 |
|------|------|
| PyCharm 2024.1 | 后端开发 IDE |
| VS Code | 前端开发 IDE（安装 Volar 插件） |
| Navicat 16 | 数据库可视化管理 |
| Git | 版本控制 |
| Node.js 18+ | 前端运行环境 |
| Python 3.12 | 后端运行环境 |
| Postman / Apifox | API 接口调试 |

---

## 四、系统总体架构设计

### 4.1 前后端分离架构说明

```
┌─────────────────────────────────────────────────────────────┐
│                        用户层（浏览器）                        │
│  ┌──────────────────────┐  ┌──────────────────────────────┐ │
│  │   前台商城（Vue3 SPA） │  │   后台管理（Vue3 SPA）         │ │
│  │   端口: 5173          │  │   端口: 5174                  │ │
│  └──────────┬───────────┘  └──────────────┬───────────────┘ │
│             │    Axios HTTP / JWT Token     │                │
└─────────────┼──────────────────────────────┼────────────────┘
              │                              │
              ▼                              ▼
┌─────────────────────────────────────────────────────────────┐
│                     Nginx 反向代理（生产环境）                  │
│            /api/* → FastAPI    /* → Vue3 静态文件              │
└─────────────────────────┬───────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────┐
│                     后端服务层（FastAPI）                      │
│  ┌─────────┐ ┌──────────┐ ┌──────────┐ ┌────────────────┐  │
│  │ Router  │→│ Service  │→│  Model   │ │  AI Service    │  │
│  │ (路由)   │ │ (业务逻辑) │ │(ORM数据层)│ │ (LangChain)   │  │
│  └─────────┘ └──────────┘ └────┬─────┘ └───────┬────────┘  │
│                                │                │           │
│                    Uvicorn ASGI Server (端口 8000)           │
└────────────────────────┬───────┼────────────────┼───────────┘
                         │       │                │
                         ▼       ▼                ▼
                   ┌──────────┐  ┌──────────┐  ┌─────────────┐
                   │  MySQL8  │  │  Redis   │  │  通义千问 API │
                   │  (主数据库)│  │  (缓存)   │  │  (大模型)    │
                   └──────────┘  └──────────┘  └─────────────┘
```

### 4.2 系统模块整体划分

```
pet-mall/
├── backend/                      # 后端项目
│   ├── app/
│   │   ├── main.py               # FastAPI 入口
│   │   ├── config.py             # 配置文件（数据库、JWT、AI Key）
│   │   ├── database.py           # Tortoise-ORM 初始化
│   │   ├── models/               # 数据模型
│   │   │   ├── user.py
│   │   │   ├── product.py
│   │   │   ├── order.py
│   │   │   ├── category.py
│   │   │   ├── address.py
│   │   │   ├── cart.py
│   │   │   ├── announcement.py
│   │   │   ├── carousel.py
│   │   │   └── admin.py
│   │   ├── schemas/              # Pydantic 请求/响应模型
│   │   │   ├── user.py
│   │   │   ├── product.py
│   │   │   ├── order.py
│   │   │   └── ...
│   │   ├── routers/              # API 路由
│   │   │   ├── auth.py           # 登录注册
│   │   │   ├── user.py           # 用户管理
│   │   │   ├── admin.py          # 管理员
│   │   │   ├── product.py        # 商品
│   │   │   ├── category.py       # 分类
│   │   │   ├── order.py          # 订单
│   │   │   ├── cart.py           # 购物车
│   │   │   ├── address.py        # 收货地址
│   │   │   ├── announcement.py   # 公告
│   │   │   ├── carousel.py       # 轮播
│   │   │   └── ai_chat.py        # AI 客服
│   │   ├── services/             # 业务逻辑层
│   │   │   ├── auth_service.py
│   │   │   ├── user_service.py
│   │   │   ├── product_service.py
│   │   │   ├── order_service.py
│   │   │   ├── cart_service.py
│   │   │   └── ai_service.py     # AI 客服核心逻辑
│   │   ├── utils/                # 工具函数
│   │   │   ├── jwt.py            # JWT 工具
│   │   │   ├── response.py       # 统一返回格式
│   │   │   ├── exception.py      # 统一异常处理
│   │   │   └── deps.py           # 依赖注入（获取当前用户等）
│   │   └── ai/                   # AI 模块
│   │       ├── chains.py         # LangChain 链定义
│   │       ├── knowledge.py      # 知识库管理
│   │       └── prompts.py        # Prompt 模板
│   ├── knowledge_base/           # 知识库文件（Markdown）
│   │   ├── pet_care.md
│   │   ├── feeding_guide.md
│   │   └── common_diseases.md
│   ├── requirements.txt
│   └── alembic/                  # 数据库迁移（可选）
│
├── frontend-user/                # 前台商城（Vue3）
│   ├── src/
│   │   ├── main.js
│   │   ├── App.vue
│   │   ├── router/               # 路由
│   │   ├── stores/               # Pinia 状态
│   │   ├── views/                # 页面
│   │   │   ├── Home.vue
│   │   │   ├── Login.vue
│   │   │   ├── Register.vue
│   │   │   ├── ProductList.vue
│   │   │   ├── ProductDetail.vue
│   │   │   ├── Cart.vue
│   │   │   ├── OrderList.vue
│   │   │   ├── OrderConfirm.vue
│   │   │   ├── UserCenter.vue
│   │   │   ├── AddressManage.vue
│   │   │   └── AiChat.vue
│   │   ├── components/           # 公共组件
│   │   ├── api/                  # Axios 封装
│   │   └── utils/                # 工具函数
│   ├── package.json
│   └── vite.config.js
│
├── frontend-admin/               # 后台管理（Vue3）
│   ├── src/
│   │   ├── views/
│   │   │   ├── Dashboard.vue     # 数据看板
│   │   │   ├── AdminManage.vue
│   │   │   ├── UserManage.vue
│   │   │   ├── CategoryManage.vue
│   │   │   ├── ProductManage.vue
│   │   │   ├── OrderManage.vue
│   │   │   ├── AnnouncementManage.vue
│   │   │   ├── CarouselManage.vue
│   │   │   └── AdminProfile.vue
│   │   ├── layout/               # 后台布局（侧边栏+顶栏）
│   │   ├── router/
│   │   ├── stores/
│   │   └── api/
│   └── package.json
│
└── deploy/                       # 部署配置
    ├── docker-compose.yml
    ├── nginx/
    │   └── nginx.conf
    └── Dockerfile.backend
```

### 4.3 数据流转逻辑

**前后端通信规范：**
- 所有 API 以 `/api/v1` 为前缀
- 请求头携带 `Authorization: Bearer <token>`
- 响应体统一格式（见第七部分）
- 文件上传使用 `multipart/form-data`
- AI 客服使用 SSE（Server-Sent Events）流式返回

---

## 五、详细功能模块设计

### 5.1 前台用户端功能模块

#### 5.1.1 用户注册与登录

**注册页面字段：**
- 用户名（必填，4-20 位，字母数字下划线）
- 密码（必填，6-20 位，需包含字母和数字）
- 确认密码（必填，与密码一致）
- 手机号（必填，11 位手机号格式校验）
- 邮箱（选填）

**登录逻辑：**
- 输入账号密码 → 后端校验 → 生成 JWT Token（有效期 2 小时）
- 前端将 Token 存储在 Pinia + localStorage
- Axios 请求拦截器自动携带 `Authorization: Bearer <token>`
- 响应拦截器捕获 401 状态码 → 自动刷新 Token 或跳转登录页

**路由守卫：**
```javascript
// router/index.js
router.beforeEach((to, from, next) => {
  const userStore = useUserStore()
  if (to.meta.requiresAuth && !userStore.token) {
    next('/login')
  } else {
    next()
  }
})
```

#### 5.1.2 首页展示

**页面布局：**
```
┌──────────────────────────────────────────┐
│  顶部导航栏：Logo | 搜索框 | 登录/购物车  │
├──────────────────────────────────────────┤
│  轮播公告区域（Element-Plus Carousel）    │
├──────────────────────────────────────────┤
│  商品分类快捷入口（图标导航）              │
├──────────────────────────────────────────┤
│  热门商品推荐（按销量排序 Top 8）          │
├──────────────────────────────────────────┤
│  最新上架商品（按创建时间排序 Top 8）      │
├──────────────────────────────────────────┤
│  底部：关于我们 | 联系方式 | 公告链接       │
└──────────────────────────────────────────┘
```

**数据加载：** 页面初始化时并行请求轮播数据、分类列表、热门商品、最新商品四个接口。

#### 5.1.3 商品浏览与搜索

**功能细节：**
- 左侧分类导航树（支持一级/二级分类）
- 顶部搜索框支持关键词模糊匹配（商品名称、描述）
- 排序选项：综合排序、价格升序、价格降序、销量排序
- 分页参数：`page=1&page_size=12`
- 商品卡片展示：主图、名称、价格、销量、库存状态

**搜索接口设计：**
```
GET /api/v1/products?category_id=1&keyword=金毛&sort=price_asc&page=1&page_size=12
```

#### 5.1.4 商品详情页

**页面元素：**
- 商品图片轮播（支持多图切换）
- 商品基本信息：名称、价格（原价/现价）、库存、销量
- 商品描述（富文本渲染）
- 用户评价列表（分页加载）
- 操作按钮：加入购物车、立即购买

**库存校验逻辑：**
- 加入购物车时校验库存 > 0
- 提交订单时再次校验库存，防止超卖
- 下单成功后扣减库存（使用数据库事务保证原子性）

#### 5.1.5 购物车

**数据存储：** 购物车数据存储在后端数据库（用户登录后），未登录状态可使用 localStorage 临时存储，登录后合并。

**功能清单：**
- 添加商品到购物车（自动合并同商品）
- 修改商品数量（最小 1，最大不超过库存）
- 删除单个商品
- 全选/取消全选
- 底部固定栏：已选商品数量、合计金额、去结算按钮

#### 5.1.6 下单与订单管理

**下单流程：**
1. 用户在购物车点击"去结算"
2. 跳转到订单确认页，展示：商品清单、收货地址选择、合计金额
3. 点击"提交订单" → 后端创建订单记录（状态：待付款）→ 扣减库存
4. 模拟支付（点击"确认付款"按钮）→ 订单状态变为"已付款"
5. 跳转到订单详情页

**订单状态流转：**
```
待付款 → 已付款 → 已发货 → 已完成
  ↓
已取消（用户主动取消，恢复库存）
```

**订单列表页：**
- 顶部 Tab 切换：全部 / 待付款 / 已付款 / 已发货 / 已完成
- 每条订单展示：订单号、商品缩略图、总金额、状态标签、操作按钮
- 待付款订单可"取消订单"或"去付款"
- 已发货订单可"确认收货"

#### 5.1.7 收货地址管理

**字段：**
- 收货人姓名（必填）
- 手机号（必填）
- 省/市/区（三级联动选择，使用 Element-Plus Cascader）
- 详细地址（必填）
- 是否默认地址（开关）

**逻辑：** 每个用户最多保存 10 个收货地址，设置新默认地址时自动取消旧默认。

#### 5.1.8 个人中心

**功能：**
- 查看个人信息（用户名、手机号、邮箱、注册时间）
- 编辑个人信息（用户名不可修改，手机号/邮箱可修改）
- 修改密码（需输入旧密码验证）

---

### 5.2 后台管理端功能模块

#### 5.2.1 后台整体布局

```
┌──────────────────────────────────────────────────────┐
│  顶栏：系统名称 | 管理员头像 | 退出登录                │
├──────────┬───────────────────────────────────────────┤
│          │                                           │
│  侧边栏   │              内容区域                      │
│  ┌─────┐ │  ┌─────────────────────────────────────┐ │
│  │数据  │ │  │                                     │ │
│  │看板  │ │  │         当前页面内容                   │ │
│  ├─────┤ │  │                                     │ │
│  │用户  │ │  │                                     │ │
│  │管理  │ │  │                                     │ │
│  ├─────┤ │  └─────────────────────────────────────┘ │
│  │商品  │ │                                           │
│  │管理  │ │                                           │
│  ├─────┤ │                                           │
│  │订单  │ │                                           │
│  │管理  │ │                                           │
│  ├─────┤ │                                           │
│  │...   │ │                                           │
│  └─────┘ │                                           │
└──────────┴───────────────────────────────────────────┘
```

#### 5.2.2 数据看板（Dashboard）

**展示数据：**
- 今日销售额、今日订单数、总用户数、总商品数（四个统计卡片）
- 近 7 天销售额折线图（ECharts）
- 近 7 天订单量柱状图（ECharts）
- 订单状态分布饼图（待付款/已付款/已发货/已完成/已取消）
- 最近 10 条订单列表

#### 5.2.3 商品管理

**商品列表页：**
- 搜索条件：商品名称、分类、上架状态
- 表格列：ID、商品图、名称、分类、价格、库存、销量、状态、操作
- 操作按钮：编辑、上架/下架、删除（需二次确认）

**新增/编辑商品表单：**
```
商品名称：[________________]（必填）
商品分类：[下拉选择_______]（必填）
商品价格：[______.00] 元  （必填，大于0）
商品库存：[______] 件     （必填，大于等于0）
商品图片：[上传区域]       （必填，支持多图，最多5张）
商品描述：[富文本编辑器]   （选填）
上架状态：[开关]           （默认关闭）
```

#### 5.2.4 订单管理

**功能：**
- 订单列表（支持按订单号、用户名、状态筛选）
- 订单详情弹窗（商品信息、收货地址、下单时间、状态变更记录）
- 发货操作（已付款订单可点击"发货"，填写快递单号）
- 订单状态修改（特殊情况管理员可手动修改状态）

#### 5.2.5 用户管理

**功能：**
- 用户列表（用户名、手机号、邮箱、注册时间、状态）
- 搜索（按用户名/手机号）
- 启用/禁用用户（禁用后用户无法登录）

#### 5.2.6 分类管理

**功能：**
- 树形表格展示（一级分类 → 二级分类）
- 新增分类（名称、父级分类、排序权重）
- 编辑/删除分类（删除前校验是否有关联商品）

#### 5.2.7 公告管理

**功能：**
- 公告列表（标题、发布时间、状态）
- 新增/编辑公告（标题 + 富文本内容 + 是否置顶）
- 发布/撤回操作

#### 5.2.8 轮播管理

**功能：**
- 轮播图列表（缩略图、标题、链接地址、排序、状态）
- 上传轮播图（图片尺寸建议 1200×400）
- 拖拽排序（可选，初期用数字输入框排序）

---

### 5.3 AI 智能客服详细设计（核心重点）

#### 5.3.1 功能概述

AI 客服是本项目的特色功能，基于 LangChain 框架 + 国内大模型 API 实现，能够回答用户的宠物喂养咨询、常见疾病护理、商品推荐等问题。

**核心能力：**
- 宠物喂养知识问答（基于知识库检索）
- 商品信息查询（"有没有金毛幼犬的狗粮？"）
- 多轮对话（记住上下文，支持追问）
- 流式输出（逐字显示，提升用户体验）

#### 5.3.2 技术实现方案

**知识库构建流程：**

```python
# 1. 准备知识库文件（Markdown 格式）
knowledge_base/
├── pet_care.md          # 宠物日常护理
├── feeding_guide.md     # 喂养指南
├── common_diseases.md   # 常见疾病
├── dog_breeds.md        # 犬种介绍
└── cat_breeds.md        # 猫种介绍

# 2. 文本分割（LangChain TextSplitter）
from langchain.text_splitter import RecursiveCharacterTextSplitter

text_splitter = RecursiveCharacterTextSplitter(
    chunk_size=500,
    chunk_overlap=50,
    separators=["\n\n", "\n", "。", "；", " "]
)

# 3. 向量化存储（通义 Embedding + ChromaDB）
from langchain_community.embeddings import DashScopeEmbeddings
from langchain_community.vectorstores import Chroma

embeddings = DashScopeEmbeddings(
    model="text-embedding-v2",
    dashscope_api_key="your-api-key"
)

vectorstore = Chroma.from_documents(
    documents=split_docs,
    embedding=embeddings,
    persist_directory="./chroma_db"
)
```

**对话链构建：**

```python
from langchain.chains import RetrievalQA
from langchain_community.llms import Tongyi  # 通义千问

# 初始化 LLM
llm = Tongyi(
    model_name="qwen-turbo",
    dashscope_api_key="your-api-key",
    streaming=True  # 开启流式输出
)

# 构建检索链
qa_chain = RetrievalQA.from_chain_type(
    llm=llm,
    chain_type="stuff",
    retriever=vectorstore.as_retriever(
        search_type="similarity",
        search_kwargs={"k": 3}
    ),
    return_source_documents=True
)
```

**系统 Prompt 模板：**

```python
SYSTEM_PROMPT = """你是一个专业的宠物顾问，名叫"小爪"。你的职责是：
1. 回答用户关于宠物喂养、护理、训练等方面的问题
2. 根据用户需求推荐合适的宠物商品
3. 如果用户问到与宠物无关的问题，礼貌地引导回宠物话题

回答要求：
- 语言亲切友好，像朋友聊天一样
- 回答简洁明了，避免过长段落
- 如果不确定答案，诚实告知并建议咨询专业兽医
- 适当使用 emoji 增加亲和力 🐾

以下是相关的知识库内容：
{context}

用户问题：{question}
"""
```

#### 5.3.3 多轮对话实现

使用 LangGraph 实现有状态的多轮对话：

```python
from langgraph.graph import StateGraph, END
from typing import TypedDict, List

class ChatState(TypedDict):
    messages: List[dict]      # 对话历史
    context: str              # 检索到的上下文
    answer: str               # 当前回答

# 定义节点
def retrieve_context(state: ChatState) -> ChatState:
    """检索知识库"""
    question = state["messages"][-1]["content"]
    docs = vectorstore.similarity_search(question, k=3)
    context = "\n".join([doc.page_content for doc in docs])
    return {"context": context}

def generate_answer(state: ChatState) -> ChatState:
    """生成回答"""
    # 组装完整 Prompt，包含历史对话
    full_prompt = build_prompt(state["messages"], state["context"])
    response = llm.invoke(full_prompt)
    return {"answer": response}

# 构建图
workflow = StateGraph(ChatState)
workflow.add_node("retrieve", retrieve_context)
workflow.add_node("generate", generate_answer)
workflow.set_entry_point("retrieve")
workflow.add_edge("retrieve", "generate")
workflow.add_edge("generate", END)

chat_app = workflow.compile()
```

#### 5.3.4 AI 客服后端 API

```python
# routers/ai_chat.py
from fastapi import APIRouter
from fastapi.responses import StreamingResponse
from pydantic import BaseModel
from typing import List

router = APIRouter(prefix="/api/v1/ai", tags=["AI客服"])

class ChatMessage(BaseModel):
    role: str          # "user" 或 "assistant"
    content: str

class ChatRequest(BaseModel):
    message: str
    history: List[ChatMessage] = []

@router.post("/chat")
async def chat_stream(request: ChatRequest):
    """AI 客服对话接口（SSE 流式返回）"""
    async def event_generator():
        async for chunk in ai_service.stream_chat(
            message=request.message,
            history=request.history
        ):
            yield f"data: {chunk}\n\n"
        yield "data: [DONE]\n\n"

    return StreamingResponse(
        event_generator(),
        media_type="text/event-stream"
    )
```

#### 5.3.5 AI 客服前端实现

```vue
<!-- views/AiChat.vue 核心逻辑 -->
<script setup>
import { ref } from 'vue'
import axios from 'axios'

const messages = ref([])
const inputText = ref('')
const isLoading = ref(false)

async function sendMessage() {
  const userMsg = { role: 'user', content: inputText.value }
  messages.value.push(userMsg)
  isLoading.value = true

  const response = await fetch('/api/v1/ai/chat', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
      message: inputText.value,
      history: messages.value.slice(-10)  // 最近10轮对话
    })
  })

  const reader = response.body.getReader()
  const assistantMsg = { role: 'assistant', content: '' }
  messages.value.push(assistantMsg)

  while (true) {
    const { done, value } = await reader.read()
    if (done) break
    const text = new TextDecoder().decode(value)
    const lines = text.split('\n')
    for (const line of lines) {
      if (line.startsWith('data: ') && line !== 'data: [DONE]') {
        assistantMsg.content += line.slice(6)
      }
    }
  }
  isLoading.value = false
}
</script>
```

#### 5.3.6 Agent 扩展路线（单 Agent → 多 Agent）

**第一阶段（MVP）：单 Agent 智能客服**
- 功能：知识库问答 + 商品推荐
- 架构：LangChain RetrievalQA Chain

**第二阶段：增加工具调用能力**
- 新增 Tool：查询订单状态、查询商品库存、查询物流信息
- 架构：LangChain Agent with Tools

```python
from langchain.agents import tool

@tool
def query_order_status(order_id: str) -> str:
    """查询订单状态，输入订单号"""
    # 查询数据库订单表
    order = Order.get_or_none(order_id=order_id)
    if order:
        return f"订单 {order_id} 状态：{order.status}，下单时间：{order.created_at}"
    return "未找到该订单"

@tool
def search_products(keyword: str) -> str:
    """搜索商品，输入商品关键词"""
    products = Product.filter(name__contains=keyword).limit(5)
    return "\n".join([f"{p.name} - ¥{p.price}" for p in products])
```

**第三阶段：多 Agent 协作**
- 客服 Agent（对话管理）
- 商品 Agent（商品推荐与查询）
- 订单 Agent（订单处理与跟踪）
- 使用 LangGraph 实现 Agent 之间的消息路由与协作

---

## 六、数据库详细设计

### 6.1 数据库环境配置

```python
# config.py
TORTOISE_ORM = {
    "connections": {
        "default": {
            "engine": "tortoise.backends.mysql",
            "credentials": {
                "host": "127.0.0.1",
                "port": 3306,
                "user": "root",
                "password": "your_password",
                "database": "pet_mall",
                "charset": "utf8mb4",
            }
        }
    },
    "apps": {
        "models": {
            "models": ["app.models", "aerich.models"],
            "default_connection": "default",
        }
    }
}
```

### 6.2 核心数据表设计

#### 6.2.1 用户表（user）

```python
# models/user.py
from tortoise import fields, models

class User(models.Model):
    id = fields.IntField(pk=True, auto_increment=True, description="用户ID")
    username = fields.CharField(max_length=20, unique=True, description="用户名")
    password_hash = fields.CharField(max_length=128, description="密码哈希值")
    phone = fields.CharField(max_length=11, unique=True, description="手机号")
    email = fields.CharField(max_length=100, null=True, description="邮箱")
    avatar = fields.CharField(max_length=255, null=True, description="头像URL")
    status = fields.IntField(default=1, description="状态：1正常 0禁用")
    created_at = fields.DatetimeField(auto_now_add=True, description="注册时间")
    updated_at = fields.DatetimeField(auto_now=True, description="更新时间")

    class Meta:
        table = "user"
        table_description = "用户表"
```

#### 6.2.2 管理员表（admin）

```python
class Admin(models.Model):
    id = fields.IntField(pk=True, auto_increment=True, description="管理员ID")
    username = fields.CharField(max_length=20, unique=True, description="管理员账号")
    password_hash = fields.CharField(max_length=128, description="密码哈希值")
    real_name = fields.CharField(max_length=30, null=True, description="真实姓名")
    phone = fields.CharField(max_length=11, null=True, description="手机号")
    role = fields.IntField(default=0, description="角色：0普通管理员 1超级管理员")
    status = fields.IntField(default=1, description="状态：1正常 0禁用")
    created_at = fields.DatetimeField(auto_now_add=True, description="创建时间")
    updated_at = fields.DatetimeField(auto_now=True, description="更新时间")

    class Meta:
        table = "admin"
        table_description = "管理员表"
```

#### 6.2.3 商品分类表（category）

```python
class Category(models.Model):
    id = fields.IntField(pk=True, auto_increment=True, description="分类ID")
    name = fields.CharField(max_length=50, description="分类名称")
    parent_id = fields.IntField(null=True, description="父分类ID，null为一级分类")
    icon = fields.CharField(max_length=255, null=True, description="分类图标URL")
    sort_order = fields.IntField(default=0, description="排序权重，越大越靠前")
    status = fields.IntField(default=1, description="状态：1启用 0禁用")
    created_at = fields.DatetimeField(auto_now_add=True, description="创建时间")

    class Meta:
        table = "category"
        table_description = "商品分类表"
```

#### 6.2.4 商品表（product）

```python
class Product(models.Model):
    id = fields.IntField(pk=True, auto_increment=True, description="商品ID")
    name = fields.CharField(max_length=100, description="商品名称")
    category_id = fields.IntField(description="所属分类ID")
    price = fields.DecimalField(max_digits=10, decimal_places=2, description="售价（元）")
    original_price = fields.DecimalField(max_digits=10, decimal_places=2, null=True, description="原价")
    stock = fields.IntField(default=0, description="库存数量")
    sales = fields.IntField(default=0, description="销量")
    main_image = fields.CharField(max_length=255, description="主图URL")
    images = fields.TextField(null=True, description="图片列表JSON")
    description = fields.TextField(null=True, description="商品描述（富文本）")
    status = fields.IntField(default=1, description="状态：1上架 0下架")
    created_at = fields.DatetimeField(auto_now_add=True, description="创建时间")
    updated_at = fields.DatetimeField(auto_now=True, description="更新时间")

    class Meta:
        table = "product"
        table_description = "商品表"
```

#### 6.2.5 收货地址表（address）

```python
class Address(models.Model):
    id = fields.IntField(pk=True, auto_increment=True, description="地址ID")
    user_id = fields.IntField(description="所属用户ID")
    receiver_name = fields.CharField(max_length=30, description="收货人姓名")
    receiver_phone = fields.CharField(max_length=11, description="收货人手机号")
    province = fields.CharField(max_length=20, description="省")
    city = fields.CharField(max_length=20, description="市")
    district = fields.CharField(max_length=20, description="区")
    detail = fields.CharField(max_length=200, description="详细地址")
    is_default = fields.BooleanField(default=False, description="是否默认地址")
    created_at = fields.DatetimeField(auto_now_add=True, description="创建时间")
    updated_at = fields.DatetimeField(auto_now=True, description="更新时间")

    class Meta:
        table = "address"
        table_description = "收货地址表"
```

#### 6.2.6 购物车表（cart）

```python
class Cart(models.Model):
    id = fields.IntField(pk=True, auto_increment=True, description="购物车ID")
    user_id = fields.IntField(description="用户ID")
    product_id = fields.IntField(description="商品ID")
    quantity = fields.IntField(default=1, description="数量")
    selected = fields.BooleanField(default=True, description="是否选中")
    created_at = fields.DatetimeField(auto_now_add=True, description="添加时间")
    updated_at = fields.DatetimeField(auto_now=True, description="更新时间")

    class Meta:
        table = "cart"
        table_description = "购物车表"
        unique_together = ("user_id", "product_id")  # 同用户同商品唯一
```

#### 6.2.7 订单表（order）

```python
class Order(models.Model):
    id = fields.IntField(pk=True, auto_increment=True, description="订单ID")
    order_no = fields.CharField(max_length=32, unique=True, description="订单编号")
    user_id = fields.IntField(description="用户ID")
    total_amount = fields.DecimalField(max_digits=10, decimal_places=2, description="订单总金额")
    status = fields.IntField(default=0, description="状态：0待付款 1已付款 2已发货 3已完成 4已取消")
    address_snapshot = fields.TextField(description="收货地址快照JSON")
    remark = fields.CharField(max_length=200, null=True, description="订单备注")
    pay_time = fields.DatetimeField(null=True, description="付款时间")
    ship_time = fields.DatetimeField(null=True, description="发货时间")
    complete_time = fields.DatetimeField(null=True, description="完成时间")
    cancel_time = fields.DatetimeField(null=True, description="取消时间")
    created_at = fields.DatetimeField(auto_now_add=True, description="下单时间")
    updated_at = fields.DatetimeField(auto_now=True, description="更新时间")

    class Meta:
        table = "order"
        table_description = "订单表"
```

#### 6.2.8 订单明细表（order_item）

```python
class OrderItem(models.Model):
    id = fields.IntField(pk=True, auto_increment=True, description="明细ID")
    order_id = fields.IntField(description="订单ID")
    product_id = fields.IntField(description="商品ID")
    product_name = fields.CharField(max_length=100, description="商品名称快照")
    product_image = fields.CharField(max_length=255, description="商品图片快照")
    price = fields.DecimalField(max_digits=10, decimal_places=2, description="下单时单价")
    quantity = fields.IntField(description="购买数量")

    class Meta:
        table = "order_item"
        table_description = "订单明细表"
```

#### 6.2.9 系统公告表（announcement）

```python
class Announcement(models.Model):
    id = fields.IntField(pk=True, auto_increment=True, description="公告ID")
    title = fields.CharField(max_length=100, description="公告标题")
    content = fields.TextField(description="公告内容（富文本）")
    is_top = fields.BooleanField(default=False, description="是否置顶")
    status = fields.IntField(default=1, description="状态：1发布 0草稿")
    created_at = fields.DatetimeField(auto_now_add=True, description="创建时间")
    updated_at = fields.DatetimeField(auto_now=True, description="更新时间")

    class Meta:
        table = "announcement"
        table_description = "系统公告表"
```

#### 6.2.10 轮播图表（carousel）

```python
class Carousel(models.Model):
    id = fields.IntField(pk=True, auto_increment=True, description="轮播图ID")
    title = fields.CharField(max_length=50, description="标题")
    image_url = fields.CharField(max_length=255, description="图片URL")
    link_url = fields.CharField(max_length=255, null=True, description="跳转链接")
    sort_order = fields.IntField(default=0, description="排序权重")
    status = fields.IntField(default=1, description="状态：1启用 0禁用")
    created_at = fields.DatetimeField(auto_now_add=True, description="创建时间")

    class Meta:
        table = "carousel"
        table_description = "轮播图表"
```

### 6.3 数据库约束与优化方案

**索引设计：**
```sql
-- 用户表索引
CREATE INDEX idx_user_phone ON user(phone);
CREATE INDEX idx_user_status ON user(status);

-- 商品表索引
CREATE INDEX idx_product_category ON product(category_id);
CREATE INDEX idx_product_status ON product(status);
CREATE INDEX idx_product_sales ON product(sales DESC);
CREATE INDEX idx_product_created ON product(created_at DESC);

-- 订单表索引
CREATE INDEX idx_order_user ON `order`(user_id);
CREATE INDEX idx_order_status ON `order`(status);
CREATE INDEX idx_order_created ON `order`(created_at DESC);
CREATE INDEX idx_order_no ON `order`(order_no);

-- 购物车索引
CREATE UNIQUE INDEX idx_cart_user_product ON cart(user_id, product_id);

-- 地址索引
CREATE INDEX idx_address_user ON address(user_id);
```

**优化建议：**
- 订单编号生成规则：`PM` + 日期 `20260702` + 6位随机数，避免连续可预测
- 收货地址在下单时做 JSON 快照存入订单表，避免地址修改影响历史订单
- 商品图片列表使用 JSON 字段存储多个图片 URL
- 高频查询（商品列表、订单列表）均需分页，默认 page_size=10~20
- 后期数据量大时可考虑对订单表按月分表

---

## 七、后端 API 接口设计

### 7.1 统一返回格式

```json
{
    "code": 200,
    "message": "success",
    "data": {}
}
```

**状态码约定：**

| code | 含义 |
|------|------|
| 200 | 成功 |
| 400 | 请求参数错误 |
| 401 | 未登录或 Token 过期 |
| 403 | 无权限（非管理员访问后台接口） |
| 404 | 资源不存在 |
| 500 | 服务器内部错误 |

**实现代码：**

```python
# utils/response.py
from typing import Any

def success(data: Any = None, message: str = "success") -> dict:
    return {"code": 200, "message": message, "data": data}

def error(message: str = "error", code: int = 400) -> dict:
    return {"code": code, "message": message, "data": None}
```

```python
# utils/exception.py
from fastapi import Request
from fastapi.responses import JSONResponse

class BizError(Exception):
    def __init__(self, message: str, code: int = 400):
        self.message = message
        self.code = code

async def biz_error_handler(request: Request, exc: BizError):
    return JSONResponse(
        status_code=200,
        content={"code": exc.code, "message": exc.message, "data": None}
    )
```

### 7.2 全部接口清单

#### 7.2.1 认证模块（auth）

| 方法 | 路径 | 说明 | 参数 | 返回 |
|------|------|------|------|------|
| POST | `/api/v1/auth/register` | 用户注册 | `username, password, phone, email?` | `{token, user_info}` |
| POST | `/api/v1/auth/login` | 用户登录 | `username, password` | `{token, user_info}` |
| POST | `/api/v1/auth/admin/login` | 管理员登录 | `username, password` | `{token, admin_info}` |
| POST | `/api/v1/auth/refresh` | 刷新 Token | Header: `Authorization` | `{token}` |

#### 7.2.2 用户模块（user）

| 方法 | 路径 | 说明 | 参数 | 返回 |
|------|------|------|------|------|
| GET | `/api/v1/user/info` | 获取个人信息 | - | `{id, username, phone, email, avatar, created_at}` |
| PUT | `/api/v1/user/info` | 修改个人信息 | `phone?, email?` | `{user_info}` |
| PUT | `/api/v1/user/password` | 修改密码 | `old_password, new_password` | `{}` |
| GET | `/api/v1/admin/users` | 用户列表（管理员） | `page, page_size, keyword?, status?` | `{list, total}` |
| PUT | `/api/v1/admin/users/{id}/status` | 启用/禁用用户 | `status: 0/1` | `{}` |

#### 7.2.3 商品分类模块（category）

| 方法 | 路径 | 说明 | 参数 | 返回 |
|------|------|------|------|------|
| GET | `/api/v1/categories` | 分类列表（前台） | - | `[{id, name, icon, children}]` |
| GET | `/api/v1/admin/categories` | 分类列表（后台） | `page, page_size` | `{list, total}` |
| POST | `/api/v1/admin/categories` | 新增分类 | `name, parent_id?, icon?, sort_order?` | `{category}` |
| PUT | `/api/v1/admin/categories/{id}` | 编辑分类 | `name?, parent_id?, icon?, sort_order?, status?` | `{category}` |
| DELETE | `/api/v1/admin/categories/{id}` | 删除分类 | - | `{}` |

#### 7.2.4 商品模块（product）

| 方法 | 路径 | 说明 | 参数 | 返回 |
|------|------|------|------|------|
| GET | `/api/v1/products` | 商品列表（前台） | `page, page_size, category_id?, keyword?, sort?` | `{list, total}` |
| GET | `/api/v1/products/{id}` | 商品详情 | - | `{product, images, category_name}` |
| GET | `/api/v1/products/hot` | 热门商品 | `limit=8` | `[{product}]` |
| GET | `/api/v1/products/new` | 最新商品 | `limit=8` | `[{product}]` |
| GET | `/api/v1/admin/products` | 商品列表（后台） | `page, page_size, keyword?, category_id?, status?` | `{list, total}` |
| POST | `/api/v1/admin/products` | 新增商品 | `name, category_id, price, stock, main_image, images?, description?, status?` | `{product}` |
| PUT | `/api/v1/admin/products/{id}` | 编辑商品 | 同新增参数 | `{product}` |
| PUT | `/api/v1/admin/products/{id}/status` | 上架/下架 | `status: 0/1` | `{}` |
| DELETE | `/api/v1/admin/products/{id}` | 删除商品 | - | `{}` |
| POST | `/api/v1/upload/image` | 上传图片 | `file: UploadFile` | `{url}` |

#### 7.2.5 购物车模块（cart）

| 方法 | 路径 | 说明 | 参数 | 返回 |
|------|------|------|------|------|
| GET | `/api/v1/cart` | 购物车列表 | - | `[{cart_item, product_info}]` |
| POST | `/api/v1/cart` | 添加到购物车 | `product_id, quantity=1` | `{cart_item}` |
| PUT | `/api/v1/cart/{id}` | 修改数量 | `quantity` | `{cart_item}` |
| PUT | `/api/v1/cart/{id}/select` | 选中/取消选中 | `selected: bool` | `{}` |
| PUT | `/api/v1/cart/select-all` | 全选/取消全选 | `selected: bool` | `{}` |
| DELETE | `/api/v1/cart/{id}` | 删除购物车项 | - | `{}` |
| DELETE | `/api/v1/cart/selected` | 删除已选中的项 | - | `{}` |

#### 7.2.6 收货地址模块（address）

| 方法 | 路径 | 说明 | 参数 | 返回 |
|------|------|------|------|------|
| GET | `/api/v1/addresses` | 地址列表 | - | `[{address}]` |
| GET | `/api/v1/addresses/{id}` | 地址详情 | - | `{address}` |
| POST | `/api/v1/addresses` | 新增地址 | `receiver_name, receiver_phone, province, city, district, detail, is_default?` | `{address}` |
| PUT | `/api/v1/addresses/{id}` | 编辑地址 | 同新增参数 | `{address}` |
| DELETE | `/api/v1/addresses/{id}` | 删除地址 | - | `{}` |
| PUT | `/api/v1/addresses/{id}/default` | 设为默认 | - | `{}` |

#### 7.2.7 订单模块（order）

| 方法 | 路径 | 说明 | 参数 | 返回 |
|------|------|------|------|------|
| POST | `/api/v1/orders` | 创建订单 | `address_id, cart_ids: [int], remark?` | `{order}` |
| GET | `/api/v1/orders` | 我的订单列表 | `page, page_size, status?` | `{list, total}` |
| GET | `/api/v1/orders/{id}` | 订单详情 | - | `{order, items}` |
| PUT | `/api/v1/orders/{id}/pay` | 模拟付款 | - | `{}` |
| PUT | `/api/v1/orders/{id}/cancel` | 取消订单 | - | `{}` |
| PUT | `/api/v1/orders/{id}/confirm` | 确认收货 | - | `{}` |
| GET | `/api/v1/admin/orders` | 订单列表（后台） | `page, page_size, order_no?, status?, username?` | `{list, total}` |
| GET | `/api/v1/admin/orders/{id}` | 订单详情（后台） | - | `{order, items, user_info}` |
| PUT | `/api/v1/admin/orders/{id}/ship` | 发货 | `express_no?` | `{}` |
| GET | `/api/v1/admin/stats` | 销量统计 | `type: day/week/month` | `{chart_data, summary}` |

#### 7.2.8 公告与轮播模块

| 方法 | 路径 | 说明 | 参数 | 返回 |
|------|------|------|------|------|
| GET | `/api/v1/announcements` | 公告列表（前台） | `page, page_size` | `{list, total}` |
| GET | `/api/v1/announcements/{id}` | 公告详情 | - | `{announcement}` |
| GET | `/api/v1/carousels` | 轮播图列表（前台） | - | `[{carousel}]` |
| GET | `/api/v1/admin/announcements` | 公告列表（后台） | `page, page_size` | `{list, total}` |
| POST | `/api/v1/admin/announcements` | 新增公告 | `title, content, is_top?, status?` | `{announcement}` |
| PUT | `/api/v1/admin/announcements/{id}` | 编辑公告 | 同新增参数 | `{announcement}` |
| DELETE | `/api/v1/admin/announcements/{id}` | 删除公告 | - | `{}` |
| GET | `/api/v1/admin/carousels` | 轮播图列表（后台） | `page, page_size` | `{list, total}` |
| POST | `/api/v1/admin/carousels` | 新增轮播图 | `title, image_url, link_url?, sort_order?` | `{carousel}` |
| PUT | `/api/v1/admin/carousels/{id}` | 编辑轮播图 | 同新增参数 | `{carousel}` |
| DELETE | `/api/v1/admin/carousels/{id}` | 删除轮播图 | - | `{}` |

#### 7.2.9 AI 客服模块

| 方法 | 路径 | 说明 | 参数 | 返回 |
|------|------|------|------|------|
| POST | `/api/v1/ai/chat` | AI 对话（SSE 流式） | `message, history?: [{role, content}]` | `text/event-stream` |

#### 7.2.10 管理员模块

| 方法 | 路径 | 说明 | 参数 | 返回 |
|------|------|------|------|------|
| GET | `/api/v1/admin/info` | 获取管理员信息 | - | `{admin_info}` |
| PUT | `/api/v1/admin/info` | 修改管理员信息 | `real_name?, phone?` | `{admin_info}` |
| PUT | `/api/v1/admin/password` | 修改密码 | `old_password, new_password` | `{}` |
| GET | `/api/v1/admin/list` | 管理员列表（超管） | `page, page_size` | `{list, total}` |
| POST | `/api/v1/admin/create` | 新增管理员（超管） | `username, password, real_name?, phone?, role?` | `{admin}` |
| DELETE | `/api/v1/admin/{id}` | 删除管理员（超管） | - | `{}` |

---

## 八、项目开发进度计划

### 8.1 总体排期（共 8 周）

| 阶段 | 周次 | 任务内容 | 产出物 |
|------|------|----------|--------|
| 第一阶段 | 第 1 周 | 环境搭建 + 数据库设计 + 后端基础框架 | 后端骨架代码、数据库建表脚本 |
| 第二阶段 | 第 2-3 周 | 后端核心模块开发（认证、用户、商品、分类） | 后端 API 接口（可联调） |
| 第三阶段 | 第 4-5 周 | 后端业务模块开发（订单、购物车、地址、公告） | 全部后端接口完成 |
| 第四阶段 | 第 3-5 周 | 前台商城页面开发（与后端并行） | 前台所有页面 |
| 第五阶段 | 第 5-6 周 | 后台管理端开发 | 后台所有页面 |
| 第六阶段 | 第 6-7 周 | AI 智能客服模块开发 | AI 客服功能可用 |
| 第七阶段 | 第 7 周 | 联调测试 + Bug 修复 | 测试报告 |
| 第八阶段 | 第 8 周 | 部署上线 + 文档完善 | 生产环境就绪 |

### 8.2 详细每日/每周任务

#### 第 1 周：项目初始化

| 日期 | 任务 | 详细内容 |
|------|------|----------|
| 周一 | 环境搭建 | 安装 Python 3.12、Node.js 18、MySQL 8、Redis；配置 PyCharm + VS Code |
| 周一 | 后端初始化 | 创建 FastAPI 项目，配置 Tortoise-ORM 连接，实现统一返回格式和异常处理 |
| 周二 | 数据库设计 | 编写所有数据表 Model，生成数据库表，插入测试数据 |
| 周三 | 后端基础模块 | 实现 JWT 工具函数、依赖注入（获取当前用户）、CORS 配置 |
| 周四 | 前端初始化 | 创建 Vue3 + Vite 项目（前台 + 后台），配置 Element Plus、Axios、Vue Router、Pinia |
| 周五 | 前端基础模块 | 实现 Axios 拦截器（Token 注入、401 处理）、路由守卫、后台 Layout 布局 |

#### 第 2 周：认证 + 用户 + 分类模块

| 日期 | 任务 | 详细内容 |
|------|------|----------|
| 周一 | 认证后端 | 用户注册、用户登录、管理员登录、Token 刷新接口 |
| 周二 | 用户后端 | 个人信息 CRUD、管理员管理用户接口 |
| 周三 | 分类后端 | 分类 CRUD 接口，支持树形结构返回 |
| 周四 | 认证 + 分类前端 | 登录/注册页面、分类管理页面（后台） |
| 周五 | 用户前端 | 个人中心页面、用户管理页面（后台） |

#### 第 3 周：商品模块 + 前台首页

| 日期 | 任务 | 详细内容 |
|------|------|----------|
| 周一 | 商品后端 | 商品 CRUD 接口、图片上传接口、热门/最新商品接口 |
| 周二 | 商品后台前端 | 商品管理页面（列表、新增/编辑表单、图片上传） |
| 周三 | 前台首页 | 首页布局、轮播展示、分类导航、热门商品推荐 |
| 周四 | 前台商品页 | 商品列表页（筛选、搜索、排序、分页）、商品详情页 |
| 周五 | 联调 | 前后端联调商品相关接口，修复问题 |

#### 第 4 周：购物车 + 收货地址

| 日期 | 任务 | 详细内容 |
|------|------|----------|
| 周一 | 购物车后端 | 购物车 CRUD 接口、全选/取消全选、批量删除 |
| 周二 | 地址后端 | 收货地址 CRUD 接口、设为默认地址 |
| 周三 | 购物车前端 | 购物车页面（商品列表、数量修改、全选、合计金额） |
| 周四 | 地址前端 | 地址管理页面（新增/编辑表单、三级联动、默认地址） |
| 周五 | 联调 | 购物车 + 地址前后端联调 |

#### 第 5 周：订单模块 + 公告轮播

| 日期 | 任务 | 详细内容 |
|------|------|----------|
| 周一 | 订单后端 | 创建订单、订单列表、订单详情、模拟付款、取消订单、确认收货 |
| 周二 | 订单后台后端 | 后台订单列表、发货接口、销量统计接口 |
| 周三 | 公告 + 轮播后端 | 公告 CRUD、轮播图 CRUD |
| 周四 | 订单前端（前台） | 订单确认页、订单列表页（Tab 切换）、订单详情页 |
| 周五 | 订单前端（后台） | 订单管理页面、数据看板（Dashboard）页面 |

#### 第 6 周：后台管理完善 + AI 客服

| 日期 | 任务 | 详细内容 |
|------|------|----------|
| 周一 | 后台完善 | 公告管理页面、轮播管理页面、管理员管理页面 |
| 周二 | AI 基础 | LangChain 环境搭建、知识库文件准备、Embedding 生成、ChromaDB 初始化 |
| 周三 | AI 后端 | AI 客服 API（RAG 检索链 + 通义千问 + SSE 流式返回） |
| 周四 | AI 前端 | AI 客服聊天界面（气泡对话、流式渲染、发送/接收） |
| 周五 | AI 优化 | 多轮对话上下文管理、Prompt 调优、异常处理 |

#### 第 7 周：联调测试

| 日期 | 任务 | 详细内容 |
|------|------|----------|
| 周一 | 功能测试 | 全流程功能测试：注册 → 浏览 → 加购 → 下单 → 付款 → 发货 → 收货 |
| 周二 | 边界测试 | 边界条件测试：库存为 0、重复下单、Token 过期、并发下单 |
| 周三 | Bug 修复 | 根据测试结果修复 Bug |
| 周四 | 性能优化 | 接口响应优化、前端首屏加载优化、图片懒加载 |
| 周五 | 回归测试 | 修复后的回归测试，确认所有功能正常 |

#### 第 8 周：部署上线

| 日期 | 任务 | 详细内容 |
|------|------|----------|
| 周一 | 部署准备 | 编写 Dockerfile、docker-compose.yml、Nginx 配置 |
| 周二 | 本地部署测试 | 本地 Docker 环境完整部署测试 |
| 周三 | 服务器部署 | 购买/配置云服务器，部署到服务器 |
| 周四 | 上线检查 | 域名配置、HTTPS 证书、环境变量检查、安全加固 |
| 周五 | 文档交付 | API 文档整理、部署文档、项目 README |

---

## 九、系统测试方案

### 9.1 功能测试用例（核心场景）

#### 9.1.1 用户注册登录

| 测试编号 | 测试场景 | 测试步骤 | 预期结果 |
|----------|----------|----------|----------|
| TC-001 | 正常注册 | 填写合法用户名、密码、手机号，点击注册 | 注册成功，自动跳转登录 |
| TC-002 | 重复用户名 | 使用已存在的用户名注册 | 提示"用户名已存在" |
| TC-003 | 密码格式错误 | 输入纯数字密码 "123456" | 提示"密码需包含字母和数字" |
| TC-004 | 正常登录 | 输入正确账号密码 | 登录成功，跳转首页 |
| TC-005 | 密码错误 | 输入错误密码 | 提示"账号或密码错误" |
| TC-006 | Token 过期 | Token 过期后请求接口 | 返回 401，自动跳转登录页 |

#### 9.1.2 购物流程

| 测试编号 | 测试场景 | 测试步骤 | 预期结果 |
|----------|----------|----------|----------|
| TC-010 | 加入购物车 | 在商品详情页点击加入购物车 | 购物车数量 +1 |
| TC-011 | 库存为 0 | 尝试将库存为 0 的商品加入购物车 | 提示"库存不足" |
| TC-012 | 修改数量超过库存 | 在购物车修改数量为 100（库存仅 5） | 自动修正为最大库存数 |
| TC-013 | 正常下单 | 选择商品 → 选地址 → 提交订单 | 订单创建成功，状态为"待付款" |
| TC-014 | 下单库存校验 | 下单时商品库存被其他人买完 | 提示"库存不足，下单失败" |
| TC-015 | 取消订单恢复库存 | 取消一个待付款订单 | 订单状态变为"已取消"，库存恢复 |

#### 9.1.3 管理员操作

| 测试编号 | 测试场景 | 测试步骤 | 预期结果 |
|----------|----------|----------|----------|
| TC-020 | 商品上架 | 新增商品并设置为上架状态 | 前台商品列表可见该商品 |
| TC-021 | 商品下架 | 将商品状态设为下架 | 前台商品列表不可见，详情页提示已下架 |
| TC-022 | 订单发货 | 对已付款订单点击发货 | 订单状态变为"已发货" |
| TC-023 | 删除有订单的分类 | 删除一个存在关联商品的分类 | 提示"该分类下存在商品，无法删除" |

### 9.2 压力测试

使用 `locust` 进行简单压力测试：

```python
# locustfile.py
from locust import HttpUser, task, between

class PetMallUser(HttpUser):
    wait_time = between(1, 3)

    def on_start(self):
        # 登录获取 Token
        response = self.client.post("/api/v1/auth/login", json={
            "username": "testuser",
            "password": "test123"
        })
        self.token = response.json()["data"]["token"]
        self.headers = {"Authorization": f"Bearer {self.token}"}

    @task(3)
    def browse_products(self):
        self.client.get("/api/v1/products?page=1&page_size=12")

    @task(2)
    def view_product_detail(self):
        self.client.get("/api/v1/products/1")

    @task(1)
    def view_cart(self):
        self.client.get("/api/v1/cart", headers=self.headers)
```

**压测指标：**

| 指标 | 目标值 |
|------|--------|
| 并发用户数 | 200 |
| 平均响应时间 | < 300ms |
| 95 分位响应时间 | < 500ms |
| 错误率 | < 1% |
| QPS（每秒请求数） | > 500 |

### 9.3 Bug 修复流程

```
发现 Bug
    ↓
提交 Issue（描述复现步骤、预期结果、实际结果）
    ↓
确认 Bug 等级（P0阻塞 / P1严重 / P2一般 / P3轻微）
    ↓
分配开发人员修复
    ↓
修复后提交代码，触发回归测试
    ↓
测试通过 → 关闭 Issue
测试未通过 → 重新修复
```

---

## 十、部署与上线方案

### 10.1 本地开发环境部署

#### 10.1.1 后端启动

```bash
# 1. 创建虚拟环境
cd backend
python3.12 -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate

# 2. 安装依赖
pip install -r requirements.txt

# 3. 创建数据库
mysql -u root -p -e "CREATE DATABASE pet_mall DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci;"

# 4. 初始化数据库表
python -m aerich init -t app.config.TORTOISE_ORM
python -m aerich init-db
python -m aerich migrate
python -m aerich upgrade

# 5. 启动后端服务
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
# 访问 API 文档：http://localhost:8000/docs
```

#### 10.1.2 前端启动

```bash
# 前台商城
cd frontend-user
npm install
npm run dev
# 访问：http://localhost:5173

# 后台管理
cd frontend-admin
npm install
npm run dev
# 访问：http://localhost:5174
```

#### 10.1.3 requirements.txt

```
fastapi==0.111.0
uvicorn[standard]==0.30.1
tortoise-orm==0.21.7
aerich==0.8.1
pydantic==2.7.4
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.9
aiofiles==24.1.0
langchain==0.2.10
langchain-community==0.2.9
langgraph==0.1.8
dashscope==1.17.1
chromadb==0.5.0
```

### 10.2 传统部署方案（Linux + Nginx）

#### 10.2.1 服务器要求

| 配置项 | 最低要求 | 推荐配置 |
|--------|----------|----------|
| 操作系统 | Ubuntu 22.04 LTS | Ubuntu 22.04 LTS |
| CPU | 2 核 | 4 核 |
| 内存 | 2 GB | 4 GB |
| 硬盘 | 40 GB SSD | 80 GB SSD |
| 带宽 | 3 Mbps | 5 Mbps |

#### 10.2.2 部署步骤

```bash
# ===== 1. 安装基础环境 =====
sudo apt update && sudo apt upgrade -y
sudo apt install -y python3.12 python3.12-venv nginx mysql-server redis-server

# ===== 2. 配置 MySQL =====
sudo mysql_secure_installation
mysql -u root -p -e "CREATE DATABASE pet_mall DEFAULT CHARACTER SET utf8mb4;"
mysql -u root -p -e "CREATE USER 'petmall'@'localhost' IDENTIFIED BY 'StrongPassword123!';"
mysql -u root -p -e "GRANT ALL PRIVILEGES ON pet_mall.* TO 'petmall'@'localhost';"

# ===== 3. 部署后端 =====
cd /var/www/pet-mall/backend
python3.12 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
# 初始化数据库
python -m aerich upgrade

# 使用 Gunicorn + Uvicorn Worker 启动
pip install gunicorn
gunicorn app.main:app -w 4 -k uvicorn.workers.UvicornWorker -b 127.0.0.1:8000 --daemon

# ===== 4. 构建前端 =====
cd /var/www/pet-mall/frontend-user
npm install && npm run build
cp -r dist/* /var/www/pet-mall/static/user/

cd /var/www/pet-mall/frontend-admin
npm install && npm run build
cp -r dist/* /var/www/pet-mall/static/admin/

# ===== 5. 配置 Nginx =====
sudo nano /etc/nginx/sites-available/pet-mall
```

**Nginx 配置：**

```nginx
server {
    listen 80;
    server_name your-domain.com;

    # 前台商城
    location / {
        root /var/www/pet-mall/static/user;
        try_files $uri $uri/ /index.html;
        index index.html;
    }

    # 后台管理
    location /admin {
        alias /var/www/pet-mall/static/admin;
        try_files $uri $uri/ /admin/index.html;
        index index.html;
    }

    # 后端 API
    location /api/ {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # SSE 流式支持
        proxy_buffering off;
        proxy_cache off;
        proxy_read_timeout 300s;
    }

    # 上传文件访问
    location /uploads/ {
        alias /var/www/pet-mall/backend/uploads/;
        expires 30d;
    }
}
```

```bash
# 启用配置
sudo ln -s /etc/nginx/sites-available/pet-mall /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl restart nginx
```

#### 10.2.3 Systemd 服务配置

```ini
# /etc/systemd/system/pet-mall.service
[Unit]
Description=PetMall Backend Service
After=network.target mysql.service

[Service]
Type=simple
User=www-data
WorkingDirectory=/var/www/pet-mall/backend
Environment="PATH=/var/www/pet-mall/backend/venv/bin"
ExecStart=/var/www/pet-mall/backend/venv/bin/gunicorn app.main:app -w 4 -k uvicorn.workers.UvicornWorker -b 127.0.0.1:8000
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
```

```bash
sudo systemctl daemon-reload
sudo systemctl enable pet-mall
sudo systemctl start pet-mall
sudo systemctl status pet-mall
```

### 10.3 Docker 部署方案

#### 10.3.1 Dockerfile（后端）

```dockerfile
# Dockerfile.backend
FROM python:3.12-slim

WORKDIR /app

# 安装系统依赖
RUN apt-get update && apt-get install -y gcc default-libmysqlclient-dev && rm -rf /var/lib/apt/lists/*

# 安装 Python 依赖
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# 复制代码
COPY . .

# 暴露端口
EXPOSE 8000

# 启动命令
CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
```

#### 10.3.2 Dockerfile（前端 - 以前台商城为例）

```dockerfile
# Dockerfile.frontend-user
FROM node:18-alpine AS builder

WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
RUN npm run build

FROM nginx:alpine
COPY --from=builder /app/dist /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
```

#### 10.3.3 docker-compose.yml

```yaml
version: "3.8"

services:
  # MySQL 数据库
  mysql:
    image: mysql:8.0
    container_name: pet-mall-mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root123456
      MYSQL_DATABASE: pet_mall
      MYSQL_USER: petmall
      MYSQL_PASSWORD: petmall123
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    command: --character-set-server=utf8mb4 --collation-server=utf8mb4_general_ci

  # 后端 API
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile.backend
    container_name: pet-mall-backend
    restart: always
    ports:
      - "8000:8000"
    environment:
      - DB_HOST=mysql
      - DB_PORT=3306
      - DB_USER=petmall
      - DB_PASSWORD=petmall123
      - DB_NAME=pet_mall
      - JWT_SECRET=your-jwt-secret-key-here
      - DASHSCOPE_API_KEY=your-dashscope-api-key
    depends_on:
      - mysql
    volumes:
      - upload_data:/app/uploads

  # 前台商城
  frontend-user:
    build:
      context: ./frontend-user
      dockerfile: Dockerfile
    container_name: pet-mall-user
    restart: always
    ports:
      - "8080:80"

  # 后台管理
  frontend-admin:
    build:
      context: ./frontend-admin
      dockerfile: Dockerfile
    container_name: pet-mall-admin
    restart: always
    ports:
      - "8081:80"

  # Nginx 反向代理（生产环境使用）
  nginx:
    image: nginx:alpine
    container_name: pet-mall-nginx
    restart: always
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./deploy/nginx/nginx.conf:/etc/nginx/nginx.conf
      - ./ssl:/etc/nginx/ssl
    depends_on:
      - backend
      - frontend-user
      - frontend-admin

volumes:
  mysql_data:
  upload_data:
```

#### 10.3.4 一键部署命令

```bash
# 构建并启动所有服务
docker-compose up -d --build

# 查看运行状态
docker-compose ps

# 查看日志
docker-compose logs -f backend

# 停止服务
docker-compose down

# 停止并删除数据卷
docker-compose down -v
```

---

## 十一、项目难点与解决方案

### 11.1 库存超卖问题

**难点：** 多个用户同时下单购买同一商品，可能导致库存扣减为负数。

**解决方案：**
```python
# services/order_service.py
async def create_order(user_id: int, address_id: int, cart_ids: list):
    async with in_transaction():
        # 使用数据库行锁（SELECT ... FOR UPDATE）防止超卖
        for cart_id in cart_ids:
            cart = await Cart.filter(id=cart_id).select_for_update().first()
            product = await Product.filter(id=cart.product_id).select_for_update().first()

            if product.stock < cart.quantity:
                raise BizError(f"商品 {product.name} 库存不足")

            product.stock -= cart.quantity
            product.sales += cart.quantity
            await product.save()

        # 创建订单...
```

### 11.2 JWT Token 自动刷新

**难点：** Token 过期后用户需要重新登录，体验差。

**解决方案：** 采用双 Token 机制 —— Access Token（2小时）+ Refresh Token（7天）。
```python
# 前端 Axios 拦截器
axios.interceptors.response.use(
    response => response,
    async error => {
        if (error.response?.status === 401 && !error.config._retry) {
            error.config._retry = true
            const { data } = await axios.post('/api/v1/auth/refresh', {
                refresh_token: localStorage.getItem('refresh_token')
            })
            localStorage.setItem('token', data.data.token)
            error.config.headers.Authorization = `Bearer ${data.data.token}`
            return axios(error.config)
        }
        return Promise.reject(error)
    }
)
```

### 11.3 AI 客服响应速度慢

**难点：** LangChain 检索 + LLM 调用链路长，首次响应可能需要 3-5 秒。

**解决方案：**
1. **流式输出：** 使用 SSE 逐字返回，用户感知等待时间大幅缩短
2. **知识库预热：** 服务启动时预加载 Embedding，避免首次查询冷启动
3. **缓存热门问答：** 对高频问题缓存回答结果（Redis，TTL 1小时）
4. **精简检索范围：** 限制 `k=3`（取 Top 3 相关文档），减少 Prompt 长度

### 11.4 前端大图上传慢

**难点：** 商品图片可能有几 MB，上传耗时长。

**解决方案：**
1. 前端使用 `el-upload` 组件，支持进度条显示
2. 后端使用 `aiofiles` 异步写入文件
3. 前端压缩图片（`compressorjs` 库）后再上传，控制在 500KB 以内
4. 后端限制单文件最大 2MB

### 11.5 二级分类递归查询

**难点：** 分类树需要递归查询子分类，SQL 直接查需要多次查询。

**解决方案：**
```python
# services/category_service.py
async def get_category_tree():
    """一次查询所有分类，内存中组装树形结构"""
    all_categories = await Category.filter(status=1).order_by("-sort_order").all()
    category_map = {c.id: {"id": c.id, "name": c.name, "icon": c.icon, "children": []}
                    for c in all_categories}
    tree = []
    for c in all_categories:
        node = category_map[c.id]
        if c.parent_id and c.parent_id in category_map:
            category_map[c.parent_id]["children"].append(node)
        else:
            tree.append(node)
    return tree
```

### 11.6 跨域问题

**难点：** 前后端分离开发时，前端 `localhost:5173` 请求后端 `localhost:8000` 会触发 CORS 限制。

**解决方案：**
```python
# main.py
from fastapi.middleware.cors import CORSMiddleware

app.add_middleware(
    CORSMiddleware,
    allow_origins=[
        "http://localhost:5173",   # 前台开发
        "http://localhost:5174",   # 后台开发
        "https://your-domain.com" # 生产环境
    ],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)
```

---

## 十二、项目总结与拓展方向

### 12.1 项目总结

本方案为智能宠物商城管理系统提供了一套完整的开发计划，覆盖了从需求分析、技术选型、架构设计、数据库设计、API 设计、开发排期到测试部署的全流程。方案的核心特点：

1. **技术栈主流且成熟：** FastAPI + Vue3 + MySQL8 是目前中小型项目最流行的技术组合，社区活跃，文档丰富，新人学习成本低
2. **AI 能力差异化：** 基于 LangChain + 国内大模型的智能客服是本项目的核心亮点，且设计了从单 Agent 到多 Agent 的渐进式扩展路线
3. **开发计划可执行：** 按周细化到每日任务，每个阶段有明确的产出物，适合作为团队开发的执行参考
4. **部署方案灵活：** 同时提供传统部署和 Docker 部署两套方案，适应不同运维能力的团队

### 12.2 后续拓展方向

| 拓展方向 | 说明 | 优先级 |
|----------|------|--------|
| 支付对接 | 接入支付宝/微信支付沙箱环境，实现真实支付流程 | 高 |
| 物流查询 | 对接快递100等物流查询 API，实时查看物流状态 | 中 |
| 商品评价 | 用户确认收货后可对商品进行评分和文字评价 | 高 |
| 优惠券系统 | 满减券、折扣券、新人券，支持发放和使用 | 中 |
| 秒杀活动 | 限时秒杀模块，使用 Redis 预减库存 + 消息队列异步下单 | 低 |
| 多语言支持 | 前端 i18n 国际化，支持中英文切换 | 低 |
| 小程序端 | 使用 uni-app 开发微信小程序端，复用后端 API | 中 |
| 消息推送 | 订单状态变更时通过 WebSocket 或短信通知用户 | 中 |
| 数据分析 | 后台增加用户行为分析、商品热度分析、复购率统计 | 低 |
| 权限细化 | 引入 RBAC 权限模型，支持自定义角色和权限分配 | 中 |

---

> **文档版本：** v1.0
> **生成日期：** 2026-07-02
> **适用范围：** 智能宠物商城管理系统开发团队
