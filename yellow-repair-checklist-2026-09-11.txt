Cake Store “🟡 建议修复”清单（2026-09-11）

原则
1. 以数据不变量、身份边界、请求幂等、状态归属和可恢复性为判断依据。
2. 每项先核对当前原始代码；已经被上一阶段修复覆盖的项目必须用回归测试证明，不重复改写。
3. 每阶段完成对应功能测试；全部阶段结束后执行后端、前端、MySQL 迁移与关键页面系统测试。

阶段一：认证、账号与输入边界
[x] Y01 bcrypt 校验/哈希移入受并发限制的线程执行器。
[x] Y02 登录按账号和 IP 分别限流，并回收过期限流主体。
[x] Y03 改密/重置密码串行校验并原子推进 token_version；拒绝 null 密码、空用户名并转换唯一冲突。
[x] Y04 GoodsUpdate 区分未提交字段与显式 null，在请求层返回 422。

阶段二：交易、地址、收藏、评价与上传
[x] Y05 加购在并发下不丢失数量，冲突补偿路径重新校验库存。
[x] Y06 购物车数量更新失败时恢复服务端值，旧响应不得覆盖新输入。
[x] Y07 一次充值确认流程复用 request_id；重复请求核对金额和方式。
[x] Y08 地址创建/更新校验姓名、电话、详细地址和省市区父子关系。
[x] Y09 默认地址更新按用户串行，避免并发操作失败。
[x] Y10 评价图片校验 URL、所有权和长度；管理员回复设置长度上限。
[x] Y11 商品评价汇总由后端返回全量平均分与总数。
[x] Y12 收藏验证商品存在、并发添加幂等，并提供分页。
[x] Y13 评价临时图片支持释放与过期回收，已引用图片不可误删。

阶段三：会话、前端身份状态与导航
[x] Y14 流式回答绑定 conversation/message，切换会话不会串写。
[x] Y15 会话和消息历史可分页访问，并在发消息时推进会话活动时间。
[x] Y16 SSE 必须收到 done；支持 AbortController，并统一处理 401。
[x] Y17 登出、401、改密和切换账号时清理用户级 store，迟到请求校验身份。
[x] Y18 路由增加角色与强制改密元信息和全局导航控制。

阶段四：Agent、知识库、索引与检索
[x] Y19 只在当前回答确属商品推荐时使用商品证据重建答案。
[x] Y20 汇总本轮全部模型 usage，并记录检索、模型和总耗时。
[x] Y21 知识上传/删除跨 MySQL 与向量库失败时补偿，保留可重建原文。
[x] Y22 前端、后端、Nginx 统一知识文件大小，代理预留 multipart 开销。
[x] Y23 全量索引先构建新版本再切换；商品任务用版本阻止旧结果覆盖。
[x] Y24 商品搜索先应用硬约束再限制候选，候选数与全库数分开。
[x] Y25 /goods/search 限制查询长度、速率和 embedding 并发，并缓存重复查询。
[x] Y26 商品问答将确定性事实与未经结构校验的模型补充明确分级。

阶段五：统计、运营、公告与审计
[x] Y27 汇总、上一窗口和日趋势共享上海时区边界，趋势合计等于汇总。
[x] Y28 运营报告绑定 goods_id/days，条件改变即失效，旧响应不能覆盖。
[x] Y29 公告分页统一 pageNum/pageSize 契约并保留 snake_case 兼容。
[x] Y30 资金/权限操作审计进入同一事务或 outbox；AI 取消同样记录审计。

阶段六：迁移、部署、种子与风险测试
[x] Y31 DDL 迁移步骤可重入；中断后可从已完成结构继续。
[x] Y32 容器 readiness 检查数据库；真实代理链验证登录限流取得客户端 IP。
[x] Y33 演示填充脚本拒绝生产环境，不跨用户借用地址，不覆盖非演示数据。
[x] T01 补 MySQL 约束/行锁、Agent 退款审计、页面请求契约和异步竞态测试。
[x] T02 各阶段功能测试全部通过。
[x] T03 最终系统测试：后端全套、前端单测与构建、MySQL 从零/升级、浏览器关键流程。

最终验证记录（2026-09-12）
1. 后端：160 passed，另有 20 subtests passed；仅保留 21 条 Tortoise pk 弃用警告。
2. 前端：2 项 Node 单测通过；Vite 生产构建通过。
3. MySQL 9.5：隔离库从零迁移、016 中断恢复，以及 011/014/016 重入执行通过。
4. 并发与一致性：购买重试、并发取消、购物车独立结算、零价订单、快照字段和权限审计事务均通过真实 MySQL 验证。
5. 浏览器：用户登录、角色路由保护、商品规格加购、购物车、登出状态清理、管理员运营页及控制台零错误均通过。
6. 部署契约：数据库 readiness 与 Docker Compose 配置校验通过；测试服务和隔离 MySQL 已正常关闭。
